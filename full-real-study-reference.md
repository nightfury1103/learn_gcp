# Full Real GCP PCA Study Set

This set contains all real crawled ExamTopics questions from 2024 to now, plus older real case-study questions from the 360-question crawl. No generated questions are included.

| Group | Real dump questions |
|---|---:|
| Altostrat Media | 6 |
| Cymbal Retail | 0 |
| EHR Healthcare | 9 |
| KnightMotives Automotive | 10 |
| Non-case 2024-now dump | 66 |

## Questions

| Ref | Case study | Keyword | Why keyword | Answer |
|---|---|---|---|---|
| 2021 / T4 Q1 | EHR Healthcare | EHR Healthcare + privacy compliance audit | I chose this cue because the case name anchors the scenario to EHR Healthcare, and 'privacy compliance audit' is the specific requirement that changes the design choice. When you see that trigger, map it to answer AB: A: Verify EHR's product usage against the list of compliant products on the Google Cloud compliance page. | B: Advise EHR to execute a Business Associate Agreement (BAA) with Google Cloud. | AB |
| 2021 / T4 Q2 | EHR Healthcare | EHR Healthcare + verified containers | I chose this cue because the case name anchors the scenario to EHR Healthcare, and 'verified containers' is the specific requirement that changes the design choice. When you see that trigger, map it to answer AD: A: Enable Binary Authorization on GKE, and sign containers as part of a CI/CD pipeline. | D: Configure Container Registry to use vulnerability scanning to confirm that there are no vulnerabilities before deploying the... | AD |
| 2021 / T4 Q4 | EHR Healthcare | EHR Healthcare + hybrid connectivity | I chose this cue because the case name anchors the scenario to EHR Healthcare, and 'hybrid connectivity' is the specific requirement that changes the design choice. When you see that trigger, map it to answer D: D: Configure two Dedicated Interconnect connections in one metro (City) and two connections in another metro, and make sure the Interconnect connections are placed in different metro zones. | D |
| 2021 / T4 Q5 | EHR Healthcare | EHR Healthcare + load has increased | I chose this cue because the case name anchors the scenario to EHR Healthcare, and 'load has increased' is the specific requirement that changes the design choice. When you see that trigger, map it to answer C: C: Turn off Pub/Sub message batching. | C |
| 2021 / T4 Q6 | EHR Healthcare | EHR Healthcare + public IP addresses | I chose this cue because the case name anchors the scenario to EHR Healthcare, and 'public IP addresses' is the specific requirement that changes the design choice. When you see that trigger, map it to answer A: A: Create an Organizational Policy with a constraint to allow external IP addresses only on the frontend Compute Engine instances. | A |
| 2021 / T4 Q7 | EHR Healthcare | EHR Healthcare + Google KubernetesEngine | I chose this cue because the case name anchors the scenario to EHR Healthcare, and 'Google KubernetesEngine' is the specific requirement that changes the design choice. When you see that trigger, map it to answer A: A: Use a private cluster with a private endpoint with master authorized networks configured. | A |
| 2024 / T1 Q197 | Non-case | cannot be deleted or overwritten + next 5 years | I chose this cue because 'cannot be deleted or overwritten + next 5 years' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Create a retention policy on the bucket for the duration of 5 years. Create a lock on the retention policy. | A |
| 2024 / T1 Q198 | Non-case | old version of the API available + same SSL and DNS records | I chose this cue because 'old version of the API available + same SSL and DNS records' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Use separate backend pools for each API path behind the load balancer | D |
| 2024 / T1 Q199 | Non-case | autoscale + memory usage exceeds 80% | I chose this cue because 'autoscale + memory usage exceeds 80%' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Change the filter to metric.label.state = ‘used’. | C |
| 2025 / T1 Q200 | Non-case | avoid data exfiltration + on-premises environment | I chose this cue because 'avoid data exfiltration + on-premises environment' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Configure VPC Service Controls and configure Private Google Access for on-promises hosts. | B |
| 2025 / T1 Q201 | Non-case | few hundred Ubuntu and Red Hat + secure and scalable | I chose this cue because 'few hundred Ubuntu and Red Hat + secure and scalable' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Set up VM Manager, and install the OS Config agent on each VM. Schedule a patch job to apply patches on each VM. | D |
| 2025 / T1 Q202 | Non-case | Salesforce + ServiceNow + Cloud SQL | I chose this cue because 'Salesforce + ServiceNow + Cloud SQL' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Leverage Application Integration to connect the services and transform the data. | B |
| 2025 / T1 Q203 | Non-case | dynamic API content + static assets + user-uploaded videos | I chose this cue because 'dynamic API content + static assets + user-uploaded videos' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer AB: A: Use an external Application Load Balancer to serve your application APIs. | B: Use Cloud CDN to serve static assets of your application. | AB |
| 2025 / T1 Q204 | Non-case | migration plan + manage costs effectively | I chose this cue because 'migration plan + manage costs effectively' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer BE: B: Configure labels and tags for the resources provisioned in Google Cloud. | E: Adopt infrastructure as code (IaC) for the cloud resources. | BE |
| 2025 / T1 Q205 | Non-case | Cloud Run service + AlloyDB database + default configurations | I chose this cue because 'Cloud Run service + AlloyDB database + default configurations' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Enable Direct VPC egress for the Cloud Run service, and send traffic directly to a VPC. | B |
| 2025 / T1 Q206 | Non-case | Identify potential vulnerabilities + Generate verifiable metadata | I chose this cue because 'Identify potential vulnerabilities + Generate verifiable metadata' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Use Cloud Build to build container images, and then trigger Artifact Analysis on images pushed to Artifact Registry. | A |
| 2025 / T1 Q207 | Non-case | private endpoint + deployments to the GKE cluster are failing | I chose this cue because 'private endpoint + deployments to the GKE cluster are failing' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Create a Cloud Build private pool that is peered with the same VPC network as your GKE cluster. Update the Cloud Deploy pipeline to use this private pool as its execution environment. | C |
| 2025 / T1 Q208 | Non-case | on-premises compute and SAP workloads + cost estimate | I chose this cue because 'on-premises compute and SAP workloads + cost estimate' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Gather data about your current environment, and leverage Google Cloud Migration Center to generate a cost estimate. | D |
| 2025 / T1 Q209 | Non-case | log data + using SQL + dashboards | I chose this cue because 'log data + using SQL + dashboards' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Enable log analytics and run queries in the linked log dataset in BigQuery. Visualize the data with Looker Studio dashboards. | C |
| 2025 / T1 Q210 | Non-case | real-time fraud detection + older than 30 days | I chose this cue because 'real-time fraud detection + older than 30 days' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Configure a Cloud Storage bucket with an Object Lifecycle Management policy to transition data from the Standard class to the Coldline class after 30 days. | D |
| 2025 / T1 Q211 | Non-case | sensitive patient notes + outside of Qatar's borders | I chose this cue because 'sensitive patient notes + outside of Qatar's borders' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Use Vertex AI Model Garden to select a Gemma model. Deploy this model to a Vertex AI Endpoint within a Google Cloud region located in Qatar. | B |
| 2025 / T1 Q212 | Non-case | rapid data center exit + monolith is exposing an API | I chose this cue because 'rapid data center exit + monolith is exposing an API' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Implement a managed API facade with Apigee to handle all requests from dependent applications on behalf of the monolith’s backend. | B |
| 2025 / T1 Q213 | Non-case | private CI/CD runners + separate Google Cloud projects | I chose this cue because 'private CI/CD runners + separate Google Cloud projects' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Create a separate VPC in each of the four projects. Connect each environment's VPC to the shared services VPC through VPC Network Peering. | A |
| 2025 / T1 Q214 | Non-case | inspected by a Cloud Armor web application firewall + before reaching the application | I chose this cue because 'inspected by a Cloud Armor web application firewall + before reaching the application' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Set the Cloud Run ingress to Allow internal traffic and Cloud Load Balancing, and use a serverless NEG backend on the load balancer | C |
| 2025 / T1 Q215 | Non-case | production environments + blocks internet ingress traffic + direct HTTPS traffic | I chose this cue because 'production environments + blocks internet ingress traffic + direct HTTPS traffic' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: At each production folder, apply a hierarchical firewall policy to deny all ingress except for HTTPS to tagged VMs. | A |
| 2025 / T1 Q216 | Non-case | non-repudiable audit trail + administrative actions | I chose this cue because 'non-repudiable audit trail + administrative actions' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Enable GKE Audit Logging to send Kubernetes API server logs to Cloud Logging, and ensure Cloud Audit Logs are enabled for the project. | C |
| 2025 / T1 Q217 | Non-case | Spanner + FinOps team is concerned | I chose this cue because 'Spanner + FinOps team is concerned' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Develop a total cost of ownership (TCO) analysis that includes operational overhead, and present it in a workshop to facilitate a decision. | C |
| 2025 / T1 Q218 | Non-case | immediately roll back + unpredictable traffic spikes | I chose this cue because 'immediately roll back + unpredictable traffic spikes' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Separate CI/CD pipelines for database schema migrations from application deployments. When deploying a new Cloud Run revision, use gradual traffic split. | C |
| 2026 / T1 Q219 | Altostrat Media | Altostrat Media + encryption keys | I chose this cue because the case name anchors the scenario to Altostrat Media, and 'encryption keys' is the specific requirement that changes the design choice. When you see that trigger, map it to answer D: D: Use customer-managed encryption keys (CMEK) for all Cloud Storage buckets storing sensitive media content. Implement fine-grained access control using IAM roles and groups to restrict access to sensitive buckets. | D |
| 2026 / T1 Q220 | Altostrat Media | Altostrat Media + occasional interruptions | I chose this cue because the case name anchors the scenario to Altostrat Media, and 'occasional interruptions' is the specific requirement that changes the design choice. When you see that trigger, map it to answer B: B: Deploy spot VM instances. | B |
| 2026 / T1 Q221 | Altostrat Media | Altostrat Media + in isolation | I chose this cue because the case name anchors the scenario to Altostrat Media, and 'in isolation' is the specific requirement that changes the design choice. When you see that trigger, map it to answer A: A: Run unit testing. | A |
| 2026 / T1 Q222 | Altostrat Media | Altostrat Media + Java-based | I chose this cue because the case name anchors the scenario to Altostrat Media, and 'Java-based' is the specific requirement that changes the design choice. When you see that trigger, map it to answer B: B: Analyze the data via Cloud Profiler. | B |
| 2026 / T1 Q223 | Altostrat Media | Altostrat Media + DDoS attacks | I chose this cue because the case name anchors the scenario to Altostrat Media, and 'DDoS attacks' is the specific requirement that changes the design choice. When you see that trigger, map it to answer C: C: Deploy Google Cloud Armor with pre-configured and custom rules for L3/L4 and L7 protection | C |
| 2026 / T1 Q224 | Altostrat Media | Altostrat Media + API calls | I chose this cue because the case name anchors the scenario to Altostrat Media, and 'API calls' is the specific requirement that changes the design choice. When you see that trigger, map it to answer C: C: Configure Quota policies. | C |
| 2026 / T1 Q225 | KnightMotives Automotive | KnightMotives + multiple locations | I chose this cue because the case name anchors the scenario to KnightMotives Automotive, and 'multiple locations' is the specific requirement that changes the design choice. When you see that trigger, map it to answer D: D: Create a fleet of GKE clusters in different regions. Deploy the app on every cluster. Configure a multi-cluster Gateway. | D |
| 2026 / T1 Q226 | KnightMotives Automotive | KnightMotives + vehicle sensor data | I chose this cue because the case name anchors the scenario to KnightMotives Automotive, and 'vehicle sensor data' is the specific requirement that changes the design choice. When you see that trigger, map it to answer C: C: Use the vehicle ID, event ID, and timestamp (in that order) as the row key. Create a column family per sensor category, and use a column qualifier for each individual sensor within its respective category. | C |
| 2026 / T1 Q227 | KnightMotives Automotive | KnightMotives + too much CPU | I chose this cue because the case name anchors the scenario to KnightMotives Automotive, and 'too much CPU' is the specific requirement that changes the design choice. When you see that trigger, map it to answer A: A: Configure resource requests per Deployment. Set resource requests slightly above the typical CPU usage observed during monitoring. | A |
| 2026 / T1 Q228 | KnightMotives Automotive | KnightMotives + two regions | I chose this cue because the case name anchors the scenario to KnightMotives Automotive, and 'two regions' is the specific requirement that changes the design choice. When you see that trigger, map it to answer D: D: Configure the Resource Location Restriction constraint organization policy at the organization level, and ensure only the allowed regions are listed. | D |
| 2026 / T1 Q229 | KnightMotives Automotive | KnightMotives + XML format | I chose this cue because the case name anchors the scenario to KnightMotives Automotive, and 'XML format' is the specific requirement that changes the design choice. When you see that trigger, map it to answer A: A: Create a Pub/Sub topic per supplier, and have HQ publish all changes related to the respective supplier in JSON format on that topic. Allow all plants to create Pub/Sub Pull subscription to receive messages for the... | A |
| 2026 / T1 Q230 | KnightMotives Automotive | KnightMotives + personally identifiable information | I chose this cue because the case name anchors the scenario to KnightMotives Automotive, and 'personally identifiable information' is the specific requirement that changes the design choice. When you see that trigger, map it to answer A: A: Store the training data in BigQuery using column-level encryption. Train the model using Confidential GKE Nodes. | A |
| 2026 / T1 Q231 | KnightMotives Automotive | KnightMotives + guaranteed bandwidth | I chose this cue because the case name anchors the scenario to KnightMotives Automotive, and 'guaranteed bandwidth' is the specific requirement that changes the design choice. When you see that trigger, map it to answer C: C: Create a Premium Ter VPand ensure a subnet is available in the region closest to a plant. Establish a Cloud Interconnect between each subnet and the local plant. | C |
| 2026 / T1 Q232 | KnightMotives Automotive | KnightMotives + modernize their landscape | I chose this cue because the case name anchors the scenario to KnightMotives Automotive, and 'modernize their landscape' is the specific requirement that changes the design choice. When you see that trigger, map it to answer B: B: Create a Shared Virtual Private Cloud (VPC). Migrate the VMs to Compute Engine in a new project. During modernization, deploy each modernized workload in a dedicated Google Cloud project using the Shared VPC. | B |
| 2026 / T1 Q233 | KnightMotives Automotive | KnightMotives + sandbox environment | I chose this cue because the case name anchors the scenario to KnightMotives Automotive, and 'sandbox environment' is the specific requirement that changes the design choice. When you see that trigger, map it to answer A: A: Create a request form where engineers can request a sandbox environment for a specific technology. Automate the creation of a project with only the relevant APIs enabled and lower the default API quota. Grant IAM r... | A |
| 2026 / T1 Q234 | KnightMotives Automotive | KnightMotives + customer behavior | I chose this cue because the case name anchors the scenario to KnightMotives Automotive, and 'customer behavior' is the specific requirement that changes the design choice. When you see that trigger, map it to answer B: B: Configure Model Monitoring, and select prediction drift detection. | B |
| 2026 / T1 Q235 | Non-case | restricting data exfiltration + Vertex AI and Workbench | I chose this cue because 'restricting data exfiltration + Vertex AI and Workbench' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Create a service perimeter and include aiplatform.googleapis.com and notebooks.googleapis.com as protected services. | D |
| 2026 / T1 Q236 | Non-case | overall duration of a request | I chose this cue because 'overall duration of a request' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Measure the Latency of your requests. | C |
| 2026 / T1 Q237 | Non-case | fully recovered and operational + different geographical region | I chose this cue because 'fully recovered and operational + different geographical region' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Deploy active managed instance groups (MIGs) in both us-west1 and us-east1, fronted by a global external HTTP(S) Load Balancer. For the database, use a cross-region read replica in us-east1, and rely on load balanc... | A |
| 2026 / T1 Q238 | Non-case | one-time migration + 60-day window | I chose this cue because 'one-time migration + 60-day window' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Order a Transfer Appliance, copy the data to the appliance using your high-speed local network, and ship it back to Google to upload the data into your Cloud Storage bucket. | D |
| 2026 / T1 Q239 | Non-case | Monday through Friday + idle more than 70% | I chose this cue because 'Monday through Friday + idle more than 70%' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Schedule the virtual machines to start and stop to match your team’s work schedule. | A |
| 2026 / T1 Q240 | Non-case | complete zonal outage + minimizing cost | I chose this cue because 'complete zonal outage + minimizing cost' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Deploy the application on a regional MIG to provide high availability across multiple zones in the primary region. | B |
| 2026 / T1 Q241 | Non-case | real time for fraud detection + batch queries | I chose this cue because 'real time for fraud detection + batch queries' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer BD: B: Use Dataflow to process the streaming data. | D: Use BigQuery for the batch analytics reports. | BD |
| 2026 / T1 Q242 | Non-case | zonal Persistent Disk + retained for at least 90 days + separate project | I chose this cue because 'zonal Persistent Disk + retained for at least 90 days + separate project' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Use the Backup and Disaster Recovery (DR) service to create a backup plan. Configure the backup plan to take daily snapshots and store them in a backup vault with a 90-day retention policy. | D |
| 2026 / T1 Q243 | Non-case | pictures and videos + page load speed | I chose this cue because 'pictures and videos + page load speed' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer BC: B: Use Memorystore tor Redis. | C: Implement CDN with the application's external HTTPS load balancer. | BC |
| 2026 / T1 Q244 | Non-case | write-heavy + relational database + multiple cloud regions | I chose this cue because 'write-heavy + relational database + multiple cloud regions' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Deploy the application to Cloud Run in multiple regions behind a global HTTPS load balancer. Use Spanner as the database. | B |
| 2026 / T1 Q245 | Non-case | self-hosted Jupyter notebooks + maximum flexibility | I chose this cue because 'self-hosted Jupyter notebooks + maximum flexibility' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Use Vertex AI for machine learning and machine learning operations (MLOps) for model deployment. | C |
| 2026 / T1 Q246 | Non-case | monolithic Python application + slow deployment cycles | I chose this cue because 'monolithic Python application + slow deployment cycles' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Propose a phased, event-driven migration to a microservices architecture. Use Pub/Sub for asynchronous communication and deploy the fraud models on Vertex AI endpoints. | C |
| 2026 / T1 Q247 | Non-case | web tier + application tier + database tier | I chose this cue because 'web tier + application tier + database tier' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Use a combination of network tags and service accounts. Apply a unique network tag and a dedicated service account to the instances in each tier. Then create specific firewall rules that allow ingress traffic based... | C |
| 2026 / T1 Q248 | Non-case | 2 petabytes + ten weeks + low latency | I chose this cue because '2 petabytes + ten weeks + low latency' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Request Transfer Appliances from Google Cloud. Transfer the data using the appliances. | B |
| 2026 / T1 Q249 | Non-case | immutable and serve as a legal record + accidentally deleted or maliciously overwritten | I chose this cue because 'immutable and serve as a legal record + accidentally deleted or maliciously overwritten' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Enable Object Versioning on the bucket. Additionally, configure the bucket with a Bucket Lock and a retention policy of seven years. | D |
| 2026 / T1 Q250 | Non-case | external auditors + temporary, read-only access + individual auditor's identity | I chose this cue because 'external auditors + temporary, read-only access + individual auditor's identity' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Apply an IAM policy binding that grants the roles/storage.objectViewer role to the Google Group. Configure this binding with a time-based IAM Condition that automatically grants access from October 1 to November 1. | A |
| 2026 / T1 Q251 | Non-case | pull request is merged + Cloud Run | I chose this cue because 'pull request is merged + Cloud Run' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Connect your repository using the Cloud Build GitHub app. Create a trigger in Cloud Build. Once a pull request is merged, trigger Cloud Build to build and deploy the application to Cloud Run. | D |
| 2026 / T1 Q252 | Non-case | validated Hardware Security Module + fully integrated Google Cloud managed service | I chose this cue because 'validated Hardware Security Module + fully integrated Google Cloud managed service' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Create a new key in Cloud Key Management Service (Cloud KMS) with the HSM protection level. | C |
| 2026 / T1 Q253 | Non-case | recovery time objective (RTO) of less than 10 minutes + recovery point objective (RPO) of 2 minutes + full regional outage | I chose this cue because 'recovery time objective (RTO) of less than 10 minutes + recovery point objective (RPO) of 2 minutes + full regional outage' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Configure the Cloud SQL instance with a cross-region read replica. In a disaster, promote the read replica to a standalone, primary instance. | C |
| 2026 / T1 Q254 | Non-case | static service account keys + must not have permissions to directly apply | I chose this cue because 'static service account keys + must not have permissions to directly apply' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Use service account impersonation in Cloud Build. Configure the pipeline to run terraform plan on pull requests, and require manual approval before running terraform apply. | B |
| 2026 / T1 Q255 | Non-case | run all your tests + continuous delivery pipeline | I chose this cue because 'run all your tests + continuous delivery pipeline' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: 1. After the developers push the code to a central repository, trigger Cloud Build to run unit tests. If all unit tests are successful, build the application container and push it to a central registry. 2. Trigger... | A |
| 2026 / T1 Q256 | Non-case | infrastructure as code + Google Kubernetes Engine (GKE) extensively | I chose this cue because 'infrastructure as code + Google Kubernetes Engine (GKE) extensively' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Set up Config Controller, and use YAML manifests to define the desired state of your Google Cloud resources. Apply these YAML configurations to provision and manage the resources. | C |
| 2026 / T1 Q257 | Non-case | Europe and Asia + slow loading static content | I chose this cue because 'Europe and Asia + slow loading static content' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer BD: B: Enable Cloud CDN for the backend service that serves the static assets, and configure it as part of a global external HTTP(S) Load Balancer. | D: Deploy the application frontend service to Compute Engine managed in... | BD |
| 2026 / T1 Q258 | Non-case | large volumes of media data + local file system | I chose this cue because 'large volumes of media data + local file system' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Use the gcsfuse command line tool to mount the Cloud Storage bucket as a local file system, and perform read/write operations in your bucket using standard file system semantics. | C |
| 2026 / T1 Q259 | Non-case | not time-sensitive + can tolerate interruptions | I chose this cue because 'not time-sensitive + can tolerate interruptions' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Deploy spot VMs with attached persistent disks and implement checkpoint mechanisms. | B |
| 2026 / T1 Q260 | Non-case | business units + Finance + Marketing + Research and Development | I chose this cue because 'business units + Finance + Marketing + Research and Development' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Create a folder for each department under the root Organization node. Apply the resource location Organization Policy on the Finance folder. Within the Marketing folder, create separate projects for mktg-prod and m... | A |
| 2026 / T1 Q261 | Non-case | new to Kubernetes + reduce operational overhead | I chose this cue because 'new to Kubernetes + reduce operational overhead' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Assess application and dependencies for containerization. Develop a migration strategy for deployment to GKE in Autopilot mode. | D |
| 2026 / T1 Q262 | Non-case | low-priority notifications + 99.9% availability | I chose this cue because 'low-priority notifications + 99.9% availability' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Implement an error budget policy based on the availability of the SLO. Create a "page” alert that triggers only when the rate of burn of the error budget predicts a full exhaustion within the next 24 hours. | C |
| 2026 / T1 Q263 | Non-case | Prometheus + low-maintenance enterprise solution | I chose this cue because 'Prometheus + low-maintenance enterprise solution' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Enable Google Cloud Managed Service for Prometheus to monitor and alert on your workloads at scale. | C |
| 2026 / T1 Q264 | Non-case | Google Kubernetes Engine (GKE) and Amazon Elastic Kubernetes Service (EKS) + multiple cloud providers | I chose this cue because 'Google Kubernetes Engine (GKE) and Amazon Elastic Kubernetes Service (EKS) + multiple cloud providers' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Utilize Config Sync as part of GKE to synchronize configurations from a centralized repository, and utilize Policy Controller to enforce policies using OPA Gatekeeper. | D |
| 2026 / T1 Q265 | Non-case | prevent container image vulnerabilities + GKE production environment | I chose this cue because 'prevent container image vulnerabilities + GKE production environment' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer BC: B: Incorporate vulnerability scanning before building container images, and use Google-maintained base images for your container deployments. | C: Enable Artifact Analysis for the container images, and stop deployment... | BC |
| 2026 / T1 Q266 | Non-case | security reports and documentation + ISO/IEC 27001 audit process | I chose this cue because 'security reports and documentation + ISO/IEC 27001 audit process' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Review the Compliance Reports Manager for information about ISO/IEC 27001 compliance and related documentation on obtaining reports through your Google Cloud account. | C |
| 2026 / T1 Q267 | Non-case | triage incidents quickly + Cloud Monitoring workspace | I chose this cue because 'triage incidents quickly + Cloud Monitoring workspace' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: 1. Navigate the predefined dashboards in the Cloud Monitoring workspace. 2. Add metrics and create alert policies. | A |
| 2026 / T1 Q268 | EHR Healthcare | EHR Healthcare + new insurance providers | I chose this cue because the case name anchors the scenario to EHR Healthcare, and 'new insurance providers' is the specific requirement that changes the design choice. When you see that trigger, map it to answer D: D: Utilize Infrastructure as Code (IaC) tools, such as Terraform, to define and manage infrastructure configurations. | D |
| 2026 / T1 Q269 | EHR Healthcare | EHR Healthcare + compromised credentials | I chose this cue because the case name anchors the scenario to EHR Healthcare, and 'compromised credentials' is the specific requirement that changes the design choice. When you see that trigger, map it to answer C: C: Configure Workload Identity Federation with Active Directory to enable authentication and authorization for applications across both Kubernetes environments. | C |
| 2026 / T1 Q270 | EHR Healthcare | EHR Healthcare + legacy relational database | I chose this cue because the case name anchors the scenario to EHR Healthcare, and 'legacy relational database' is the specific requirement that changes the design choice. When you see that trigger, map it to answer A: A: Use the Database Migration Service (DMS) to migrate the MySQL databases to Cloud SQL. | A |
| 2026 / T1 Q271 | Non-case | more than 2 TB + resume where it left off | I chose this cue because 'more than 2 TB + resume where it left off' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Utilize Storage Transfer Service (STS) to migrate objects to the destination bucket in the target region. Once the transfer is complete, delete the original source bucket objects. | B |
| 2026 / T1 Q272 | Non-case | interconnected virtual machines + identify dependencies + total cost of ownership | I chose this cue because 'interconnected virtual machines + identify dependencies + total cost of ownership' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Use the Google Cloud Migration Center to perform an automated discovery and assessment of the on-premises environment. | B |
| 2026 / T1 Q273 | Non-case | similar dashboard + minimize time and effort | I chose this cue because 'similar dashboard + minimize time and effort' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Export your dashboard to a JSON file and share it with the Ops team. | A |
| 2026 / T1 Q274 | Non-case | tightly coupled dependencies + "all-at-once" migration | I chose this cue because 'tightly coupled dependencies + "all-at-once" migration' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Use dependency mapping to group application components into waves, create a phased migration plan, and incorporate performance testing for each wave. | D |
| 2026 / T1 Q275 | Non-case | unpredictable flash sales + automatically handle high traffic | I chose this cue because 'unpredictable flash sales + automatically handle high traffic' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Deploy the application as a Cloud Run service. Configure request-based auto-scaling and use gradual rollouts by splitting traffic between revisions. | C |
| 2026 / T1 Q276 | Non-case | logs are not correlated + downstream services | I chose this cue because 'logs are not correlated + downstream services' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Implement Cloud Trace by ensuring the traceparent header is propagated between microservice calls to link logs to a single trace. | A |
| 2026 / T1 Q277 | Non-case | GPU capacity is always available + production and development projects | I chose this cue because 'GPU capacity is always available + production and development projects' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Create an owner project with shared reservations, and then configure the production project and development project as consumer projects to consume capacity. | C |
| 2026 / T1 Q278 | Non-case | zone becomes unavailable + recreate the lost capacity | I chose this cue because 'zone becomes unavailable + recreate the lost capacity' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Create a single regional managed instance group (MIG) configured to span all three zones. Place a regional external Application Load Balancer in front of the MIG, and configure its backend service with a health check. | C |
| 2026 / T1 Q279 | Non-case | corporate identity + audit trails + simple to operate | I chose this cue because 'corporate identity + audit trails + simple to operate' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Enable uniform bucket-level access on the buckets, and grant predefined Storage IAM roles to the Google Groups. | B |
| 2026 / T1 Q280 | Non-case | hundreds of Compute Engine virtual machines + concurrent, low-latency read and write access | I chose this cue because 'hundreds of Compute Engine virtual machines + concurrent, low-latency read and write access' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Create a Filestore Enterprise instance in the same region as the VMs. Mount the Filestore file share on each Compute Engine VM. | B |
| 2026 / T1 Q281 | Non-case | pre-approved architectural patterns + self-service | I chose this cue because 'pre-approved architectural patterns + self-service' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Use Service Catalog to define, govern, and offer a portfolio of approved products. Include Terraform scripts into Service Catalog items. | B |
