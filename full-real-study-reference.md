# Full Real GCP PCA Study Set

This set contains all real crawled ExamTopics questions from 2024 to now, plus older real case-study questions from the 360-question crawl. No generated questions are included.

| Group | Real dump questions |
|---|---:|
| Altostrat Media | 6 |
| Cymbal Retail | 0 |
| EHR Healthcare | 9 |
| KnightMotives Automotive | 10 |
| Non-case 2024-now dump | 66 |

## Questions

| Ref | Case study | Keyword | Why keyword | Answer |
|---|---|---|---|---|
| 2021 / T4 Q1 | EHR Healthcare | EHR Healthcare + privacy compliance audit | Healthcare compliance is not only a technical control question. The audit trigger means you need to confirm Google Cloud services are covered for the workload and sign the healthcare legal agreement; app auth or monitoring tools do not satisfy that audit requirement. | AB |
| 2021 / T4 Q2 | EHR Healthcare | EHR Healthcare + verified containers | Verified containers points to a deployment gate, not just image storage. Binary Authorization enforces signed images at deploy time, and vulnerability scanning checks the image before it is trusted for GKE. | AD |
| 2021 / T4 Q4 | EHR Healthcare | EHR Healthcare + hybrid connectivity | Production hybrid connectivity needs redundancy across physical locations. Two Dedicated Interconnect connections in separate metros and zones avoids a single interconnect or metro failure taking down EHR connectivity. | D |
| 2021 / T4 Q5 | EHR Healthcare | EHR Healthcare + load has increased | The application is timing out but Pub/Sub publish calls are not failing, so the issue is publish latency. Message batching can wait before sending; turning it off reduces delay, while retries or subscriber mode do not target publish latency. | C |
| 2021 / T4 Q6 | EHR Healthcare | EHR Healthcare + public IP addresses | The failure mode is accidental assignment of external IP addresses. An Organization Policy prevents that configuration from being created, which is stronger than relying on firewall cleanup after a public IP already exists. | A |
| 2021 / T4 Q7 | EHR Healthcare | EHR Healthcare + Google KubernetesEngine | Reducing GKE attack surface means hiding the control plane and nodes from public access. A private cluster with private endpoint and authorized networks matches that goal; public endpoint designs leave more exposed surface. | A |
| 2024 / T1 Q197 | Non-case | cannot be deleted or overwritten + next 5 years | The phrase cannot be deleted or overwritten for five years is a WORM-retention requirement. A locked bucket retention policy enforces immutability; encryption keys or org constraints do not stop object deletion/overwrite for a fixed period. | A |
| 2024 / T1 Q198 | Non-case | old version of the API available + same SSL and DNS records | Keeping the same SSL and DNS while serving old and new APIs means traffic should split behind the existing HTTPS load balancer. Separate backend pools by path preserve the endpoint; a new load balancer or client endpoint breaks the requirement. | D |
| 2024 / T1 Q199 | Non-case | autoscale + memory usage exceeds 80% | Autoscaling on memory requires the metric to represent used memory. The filter must select state=used; free memory or the wrong metric makes the autoscaler watch the wrong signal. | C |
| 2025 / T1 Q200 | Non-case | avoid data exfiltration + on-premises environment | The threat is BigQuery data exfiltration from hybrid/on-prem access. VPC Service Controls create the service perimeter, and Private Google Access lets private hosts reach Google APIs without opening a public exfiltration path. | B |
| 2025 / T1 Q201 | Non-case | few hundred Ubuntu and Red Hat + secure and scalable | Hundreds of Linux VMs need repeatable OS patch management. VM Manager with the OS Config agent is the managed fleet patching tool; ad hoc scripts or Cloud Build jobs do not give scalable VM patch governance. | D |
| 2025 / T1 Q202 | Non-case | Salesforce + ServiceNow + Cloud SQL | Salesforce, ServiceNow, and Cloud SQL require application integration with mapping and transformation. Application Integration is built for connecting SaaS and database systems; Pub/Sub alone moves messages but does not model the business integration. | B |
| 2025 / T1 Q203 | Non-case | dynamic API content + static assets + user-uploaded videos | The workload has different content types. Dynamic API calls need a global external Application Load Balancer, while static assets should be cached with Cloud CDN; one storage bucket or Memorystore does not solve both delivery paths. | AB |
| 2025 / T1 Q204 | Non-case | migration plan + manage costs effectively | Post-migration cost control depends on making resources classifiable and repeatable. Labels/tags give cost attribution, and infrastructure as code keeps deployments standardized so cost governance is not manual after the fact. | BE |
| 2025 / T1 Q205 | Non-case | Cloud Run service + AlloyDB database + default configurations | A default AlloyDB instance is private, so Cloud Run needs private VPC connectivity. Direct VPC egress sends the service traffic into the VPC where the database is reachable. | B |
| 2025 / T1 Q206 | Non-case | Identify potential vulnerabilities + Generate verifiable metadata | The build must both find vulnerabilities and produce trusted metadata. Cloud Build plus Artifact Analysis ties scanning and provenance to the image build/push process instead of discovering issues after deployment. | A |
| 2025 / T1 Q207 | Non-case | private endpoint + deployments to the GKE cluster are failing | A private GKE endpoint cannot be reached from a public deploy runner. A Cloud Build private pool peered to the cluster VPC gives Cloud Deploy a private network path to the cluster API. | C |
| 2025 / T1 Q208 | Non-case | on-premises compute and SAP workloads + cost estimate | The question asks for a Google-recommended, quick estimate for current on-prem and SAP workloads. Migration Center is the assessment and estimation tool; spreadsheet-style estimates miss discovery and sizing data. | D |
| 2025 / T1 Q209 | Non-case | log data + using SQL + dashboards | The team wants SQL over Cloud Logging data and dashboards. Log Analytics links logs to a BigQuery-backed dataset, and Looker Studio can visualize those SQL query results. | C |
| 2025 / T1 Q210 | Non-case | real-time fraud detection + older than 30 days | Fresh transaction data serves real-time fraud detection, but older data is for long-term history. Lifecycle transition after 30 days keeps hot data in Standard and moves colder data to Coldline for cost control. | D |
| 2025 / T1 Q211 | Non-case | sensitive patient notes + outside of Qatar's borders | The hard constraint is that patient-note content cannot leave Qatar. Deploying a Gemma model in a Qatar region satisfies data residency while still using a capable pretrained model. | B |
| 2025 / T1 Q212 | Non-case | rapid data center exit + monolith is exposing an API | A rapid data-center exit means avoid rewriting the monolith first. An Apigee facade shields dependent applications from backend changes, letting the monolith move while API consumers stay stable. | B |
| 2025 / T1 Q213 | Non-case | private CI/CD runners + separate Google Cloud projects | Private runners and Artifact Registry live in shared services, while environments are separate projects. Peering each environment VPC to shared services gives private access without merging all projects into one network. | A |
| 2025 / T1 Q214 | Non-case | inspected by a Cloud Armor web application firewall + before reaching the application | The key is forcing every request through Cloud Armor on the load balancer. Setting Cloud Run ingress to internal and load-balancer traffic blocks direct run.app access and keeps inspection in the path. | C |
| 2025 / T1 Q215 | Non-case | production environments + blocks internet ingress traffic + direct HTTPS traffic | The controls must apply to all production projects and not be overwritten by project teams. Hierarchical firewall policy at the production folder enforces default ingress denial while allowing tagged HTTPS exceptions. | A |
| 2025 / T1 Q216 | Non-case | non-repudiable audit trail + administrative actions | The audit trail must show administrative actions in GKE, including kubectl and cluster changes. GKE Audit Logging and Cloud Audit Logs capture Kubernetes API and Google Cloud admin events from day one. | C |
| 2025 / T1 Q217 | Non-case | Spanner + FinOps team is concerned | This is a decision conflict, not only a database selection. A TCO analysis compares Spanner operational savings against direct cost, so the team can justify the architecture instead of choosing the cheapest line item. | C |
| 2025 / T1 Q218 | Non-case | immediately roll back + unpredictable traffic spikes | Cloud Run revisions can roll back quickly, but database schema changes cannot be treated the same way. Separating schema migrations from app deployments and using traffic splitting keeps rollback practical. | C |
| 2026 / T1 Q219 | Altostrat Media | Altostrat Media + encryption keys | Altostrat stores sensitive media and needs control over encryption keys. CMEK plus IAM groups gives customer-controlled key management and access separation; default encryption alone does not meet that sensitivity signal. | D |
| 2026 / T1 Q220 | Altostrat Media | Altostrat Media + occasional interruptions | The workload can handle occasional interruptions, which is the signal for Spot VMs. Spot capacity reduces compute cost for interruptible processing; it would be wrong for stateful or always-on user traffic. | B |
| 2026 / T1 Q221 | Altostrat Media | Altostrat Media + in isolation | Testing code in isolation is exactly unit testing. Integration, load, or end-to-end tests validate broader systems, but isolated component behavior maps to unit tests. | A |
| 2026 / T1 Q222 | Altostrat Media | Altostrat Media + Java-based | The cue is a Java application performance issue. Cloud Profiler is the tool for finding CPU and memory hotspots in running code; logging or monitoring alone shows symptoms, not code-level bottlenecks. | B |
| 2026 / T1 Q223 | Altostrat Media | Altostrat Media + DDoS attacks | DDoS attacks against internet-facing services call for edge protection. Cloud Armor provides managed WAF and DDoS rules before traffic reaches the application. | C |
| 2026 / T1 Q224 | Altostrat Media | Altostrat Media + API calls | The problem is controlling excessive or abusive API calls. Quota policies directly limit request consumption; authentication or monitoring alone does not enforce usage ceilings. | C |
| 2026 / T1 Q225 | KnightMotives Automotive | KnightMotives + multiple locations | Multiple locations and a need to serve reliably across clusters point to multi-cluster GKE. A fleet with multi-cluster Gateway can route users to healthy regional clusters instead of relying on one cluster. | D |
| 2026 / T1 Q226 | KnightMotives Automotive | KnightMotives + vehicle sensor data | Vehicle sensor data is high-volume time-series style data. A Bigtable row key using vehicle ID, event ID, and timestamp supports efficient lookups and stores sensor categories cleanly in column families. | C |
| 2026 / T1 Q227 | KnightMotives Automotive | KnightMotives + too much CPU | CPU contention in Kubernetes should be handled with resource requests. Setting requests slightly above observed usage gives the scheduler enough information without blindly overprovisioning. | A |
| 2026 / T1 Q228 | KnightMotives Automotive | KnightMotives + two regions | Only two regions are allowed, so the control should be preventative and organization-wide. Resource Location Restriction blocks deployments outside approved regions across projects. | D |
| 2026 / T1 Q229 | KnightMotives Automotive | KnightMotives + XML format | Supplier updates need to be decoupled and normalized for plants. Pub/Sub topics per supplier with JSON payloads let plants subscribe independently without tight coupling to supplier formats. | A |
| 2026 / T1 Q230 | KnightMotives Automotive | KnightMotives + personally identifiable information | The training data contains personally identifiable information. Column-level encryption protects sensitive fields, and Confidential GKE Nodes reduce exposure while training uses that data. | A |
| 2026 / T1 Q231 | KnightMotives Automotive | KnightMotives + guaranteed bandwidth | Guaranteed bandwidth to plants is a connectivity requirement, not an internet routing problem. Cloud Interconnect provides dedicated capacity; VPN or best-effort internet paths do not guarantee throughput. | C |
| 2026 / T1 Q232 | KnightMotives Automotive | KnightMotives + modernize their landscape | Modernization across a legacy landscape needs shared networking plus workload isolation. Shared VPC centralizes network control while separate projects let each modernized workload move independently. | B |
| 2026 / T1 Q233 | KnightMotives Automotive | KnightMotives + sandbox environment | Engineers need sandbox access without creating shadow IT. Automating constrained project creation with approved APIs and low quotas gives experimentation while preventing uncontrolled production-like projects. | A |
| 2026 / T1 Q234 | KnightMotives Automotive | KnightMotives + customer behavior | The model can become stale as customer behavior changes over time. Vertex AI Model Monitoring with prediction drift detection watches input/prediction changes so retraining can be triggered. | B |
| 2026 / T1 Q235 | Non-case | restricting data exfiltration + Vertex AI and Workbench | Restricting data exfiltration around Vertex AI and Workbench calls for a service perimeter. VPC Service Controls protects the AI services from unauthorized data movement outside the perimeter. | D |
| 2026 / T1 Q236 | Non-case | overall duration of a request | Overall request duration is latency. Throughput, query count, or request size are useful metrics, but they do not answer how long a request takes end to end. | C |
| 2026 / T1 Q237 | Non-case | fully recovered and operational + different geographical region | Business continuity requires running again in another region after a regional failure. Active MIGs behind a global load balancer plus a cross-region database replica provide application and database failover. | A |
| 2026 / T1 Q238 | Non-case | one-time migration + 60-day window | A 500 TB transfer over shared 1 Gbps internet will not fit the window safely. Transfer Appliance moves the bulk data offline without consuming the business network. | D |
| 2026 / T1 Q239 | Non-case | Monday through Friday + idle more than 70% | The environments are idle outside business hours. Scheduling VM start and stop around Monday-Friday working hours cuts cost without redesigning the workstations. | A |
| 2026 / T1 Q240 | Non-case | complete zonal outage + minimizing cost | Surviving a zone outage at minimum cost is a regional high-availability pattern. A regional MIG spreads instances across zones without the expense of multi-region active-active. | B |
| 2026 / T1 Q241 | Non-case | real time for fraud detection + batch queries | The same transaction stream supports real-time fraud and batch analytics. Dataflow handles streaming processing, while BigQuery is the managed engine for complex batch reporting queries. | BD |
| 2026 / T1 Q242 | Non-case | zonal Persistent Disk + retained for at least 90 days + separate project | The data sits on a zonal disk and backups must survive zone/project issues for 90 days. Backup and DR with a backup vault provides managed retention and isolated backup storage. | D |
| 2026 / T1 Q243 | Non-case | pictures and videos + page load speed | Pictures and videos slow page loads, and product pages may also repeatedly read backend data. CDN reduces static media latency, while Memorystore can reduce repeated dynamic reads. | BC |
| 2026 / T1 Q244 | Non-case | write-heavy + relational database + multiple cloud regions | Write-heavy relational data across multiple regions with low operations points to Spanner. Cloud Run can run the stateless app globally, and Spanner handles multi-region relational consistency. | B |
| 2026 / T1 Q245 | Non-case | self-hosted Jupyter notebooks + maximum flexibility | Self-hosted notebooks create operational burden and inconsistent ML handoff. Vertex AI gives managed notebooks, training, deployment, and MLOps integration in one platform. | C |
| 2026 / T1 Q246 | Non-case | monolithic Python application + slow deployment cycles | The monolith is slowing delivery, but a full rewrite is risky. A phased event-driven migration with Pub/Sub allows services to split off gradually, and Vertex AI hosts the fraud models separately. | C |
| 2026 / T1 Q247 | Non-case | web tier + application tier + database tier | The three tiers need precise east-west traffic rules. Combining network tags and service accounts lets firewall rules identify both tier role and instance identity. | C |
| 2026 / T1 Q248 | Non-case | 2 petabytes + ten weeks + low latency | Two petabytes in ten weeks is too large for ordinary online transfer. Transfer Appliance is designed for secure bulk offline transfer when time and volume make network transfer impractical. | B |
| 2026 / T1 Q249 | Non-case | immutable and serve as a legal record + accidentally deleted or maliciously overwritten | The bucket needs both recovery from overwrite/delete and legal immutability. Object Versioning keeps recoverable prior versions, while Bucket Lock plus retention prevents deletion/modification for seven years. | D |
| 2026 / T1 Q250 | Non-case | external auditors + temporary, read-only access + individual auditor's identity | Auditors need temporary read-only access tied to individual identities. An IAM binding on the Google Group with a time-based IAM Condition grants access only during the audit window. | A |
| 2026 / T1 Q251 | Non-case | pull request is merged + Cloud Run | The trigger event is a merged pull request to Cloud Run. Cloud Build GitHub App triggers connect the repository event directly to build and deploy automation. | D |
| 2026 / T1 Q252 | Non-case | validated Hardware Security Module + fully integrated Google Cloud managed service | The key requirement is HSM-backed key generation and storage in a managed service. Cloud KMS with HSM protection level gives managed lifecycle with validated hardware protection. | C |
| 2026 / T1 Q253 | Non-case | recovery time objective (RTO) of less than 10 minutes + recovery point objective (RPO) of 2 minutes + full regional outage | The RTO/RPO and full regional outage require a warm database copy in another region. A cross-region Cloud SQL read replica can be promoted quickly with less operational complexity than self-managed replication. | C |
| 2026 / T1 Q254 | Non-case | static service account keys + must not have permissions to directly apply | The pipeline must avoid static keys and keep developers from applying production changes. Cloud Build service account impersonation plus approval gates separates proposal from privileged apply. | B |
| 2026 / T1 Q255 | Non-case | run all your tests + continuous delivery pipeline | The requirement is to run tests as part of continuous delivery before releasing the container. Cloud Build can run unit tests, then build and push only after tests pass. | A |
| 2026 / T1 Q256 | Non-case | infrastructure as code + Google Kubernetes Engine (GKE) extensively | The team wants infrastructure as code for Google Cloud resources around GKE. Config Controller lets them manage cloud resources declaratively with YAML instead of manual provisioning. | C |
| 2026 / T1 Q257 | Non-case | Europe and Asia + slow loading static content | Users far from us-central1 are waiting on static content. Cloud CDN caches static assets globally, and deploying frontend closer to users reduces the latency path. | BD |
| 2026 / T1 Q258 | Non-case | large volumes of media data + local file system | The application requires filesystem semantics while data remains in Cloud Storage. gcsfuse mounts the bucket as a file system, avoiding a separate data-copy architecture. | C |
| 2026 / T1 Q259 | Non-case | not time-sensitive + can tolerate interruptions | The batch accelerator workload is not time-sensitive and can be interrupted. Spot VMs lower GPU cost, and checkpointing/persistent disks protect progress when preemptions happen. | B |
| 2026 / T1 Q260 | Non-case | business units + Finance + Marketing + Research and Development | Each business unit has different governance needs. Folder hierarchy, resource-location policies, separate projects, and organization-level viewer access apply controls at the right scope. | A |
| 2026 / T1 Q261 | Non-case | new to Kubernetes + reduce operational overhead | The team is new to Kubernetes and wants low operations. Assessing/containerizing first, then using GKE Autopilot, reduces cluster management while still adopting Kubernetes. | D |
| 2026 / T1 Q262 | Non-case | low-priority notifications + 99.9% availability | Alert fatigue should be solved by paging only on user-impacting SLO risk. Error-budget burn alerts identify when availability is being consumed too fast, instead of sending low-value noise. | C |
| 2026 / T1 Q263 | Non-case | Prometheus + low-maintenance enterprise solution | The team already uses Prometheus and wants low-maintenance scale. Managed Service for Prometheus keeps PromQL compatibility while Google runs the monitoring backend. | C |
| 2026 / T1 Q264 | Non-case | Google Kubernetes Engine (GKE) and Amazon Elastic Kubernetes Service (EKS) + multiple cloud providers | The environment spans GKE and EKS, so consistency must come from centralized config and policy. Config Sync and Policy Controller enforce desired state and guardrails across clusters. | D |
| 2026 / T1 Q265 | Non-case | prevent container image vulnerabilities + GKE production environment | The goal is to stop vulnerable images before production. Scanning early and using Google-maintained base images reduces known vulnerabilities before images reach GKE. | BC |
| 2026 / T1 Q266 | Non-case | security reports and documentation + ISO/IEC 27001 audit process | The ask is evidence for an ISO/IEC 27001 audit, not implementing the controls from scratch. Compliance Reports Manager is where Google Cloud compliance reports and documentation are obtained. | C |
| 2026 / T1 Q267 | Non-case | triage incidents quickly + Cloud Monitoring workspace | Fast incident triage starts from existing GKE visibility. Predefined Cloud Monitoring dashboards provide immediate cluster signals, and alerts/metrics add operational action. | A |
| 2026 / T1 Q268 | EHR Healthcare | EHR Healthcare + new insurance providers | EHR must scale onboarding of new insurance providers and environments. Terraform/IaC makes infrastructure repeatable and reviewable instead of hand-building each provider setup. | D |
| 2026 / T1 Q269 | EHR Healthcare | EHR Healthcare + compromised credentials | The risk is compromised credentials across hybrid Kubernetes environments. Workload Identity Federation with Active Directory lets workloads use federated identity without long-lived secrets. | C |
| 2026 / T1 Q270 | EHR Healthcare | EHR Healthcare + legacy relational database | The source is a legacy MySQL-style relational database and the target is managed Google Cloud SQL. Database Migration Service is the managed path for that database migration. | A |
| 2026 / T1 Q271 | Non-case | more than 2 TB + resume where it left off | The transfer is over 2 TB and must resume after interruption while copying only changes. Storage Transfer Service handles large object transfers with restart and incremental behavior. | B |
| 2026 / T1 Q272 | Non-case | interconnected virtual machines + identify dependencies + total cost of ownership | Before migrating interconnected VMs, you need dependency mapping and cost estimates. Migration Center discovers the on-prem environment and produces assessment/TCO data for planning. | B |
| 2026 / T1 Q273 | Non-case | similar dashboard + minimize time and effort | The Ops team wants the same dashboard with minimal effort. Exporting the dashboard JSON lets them recreate it directly instead of rebuilding widgets manually. | A |
| 2026 / T1 Q274 | Non-case | tightly coupled dependencies + "all-at-once" migration | Tightly coupled tiers make a big-bang migration risky. Dependency mapping and migration waves let each group move with validation before final production cutover. | D |
| 2026 / T1 Q275 | Non-case | unpredictable flash sales + automatically handle high traffic | Flash sales create sudden traffic spikes for a stateless container app. Cloud Run request-based autoscaling handles bursts, and gradual rollouts reduce release risk. | C |
| 2026 / T1 Q276 | Non-case | logs are not correlated + downstream services | The problem is losing the path between service logs. Propagating the traceparent header lets Cloud Trace connect frontend and downstream service events into one trace. | A |
| 2026 / T1 Q277 | Non-case | GPU capacity is always available + production and development projects | GPU availability must be reserved for production but shareable with development. Shared reservations with owner/consumer projects reserve capacity while controlling who can consume it. | C |
| 2026 / T1 Q278 | Non-case | zone becomes unavailable + recreate the lost capacity | If a zone fails, capacity must be recreated in remaining zones automatically. A regional MIG spans zones and self-heals behind a regional load balancer. | C |
| 2026 / T1 Q279 | Non-case | corporate identity + audit trails + simple to operate | The decisive requirement is identity-based, auditable, scalable access for bucket data. Uniform bucket-level access plus IAM roles on Google Groups keeps access tied to corporate identities without per-object ACL sprawl. | B |
| 2026 / T1 Q280 | Non-case | hundreds of Compute Engine virtual machines + concurrent, low-latency read and write access | Hundreds of VMs need simultaneous low-latency reads and writes to one shared file system. Filestore Enterprise provides regional managed NFS performance for that shared POSIX-style access pattern. | B |
| 2026 / T1 Q281 | Non-case | pre-approved architectural patterns + self-service | Teams need safe self-service because they lack deep cloud-native expertise. Service Catalog packages approved Terraform-backed patterns so teams deploy governed architectures without inventing them each time. | B |
