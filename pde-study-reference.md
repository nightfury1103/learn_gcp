# GCP Professional Data Engineer Study Set

This set contains real crawled ExamTopics Professional Data Engineer questions from 2024 to now. No generated questions are included.

| Group | Questions |
|---|---:|
| Professional Data Engineer dump (2024+) | 165 |

## Questions

| Ref | Keyword | Why keyword | Answer |
|---|---|---|---|
| 2024 / T1 Q15 | You need to store and analyze social media postings in Google BigQuery at a rate of 10,000 messages per | I chose this cue because 'You need to store and analyze social media postings in Google BigQuery at a rate of 10,000 messages per' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Estimate the average latency for data availability after streaming inserts, and always run queries after waiting twice as long. | D |
| 2024 / T1 Q19 | Your company's on-premises Apache Hadoop servers are approaching end-of-life, and IT has decided to migrate | I chose this cue because 'Your company's on-premises Apache Hadoop servers are approaching end-of-life, and IT has decided to migrate' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Put the data into Google Cloud Storage. | A |
| 2024 / T1 Q44 | You are deploying a new storage system for your mobile application, which is a media streaming service. You | I chose this cue because 'You are deploying a new storage system for your mobile application, which is a media streaming service. You' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Manually configure the index in your index config as follows: | A |
| 2024 / T1 Q62 | Your company receives both batch- and stream-based event data. You want to process the data using Google | I chose this cue because 'Your company receives both batch- and stream-based event data. You want to process the data using Google' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Use watermarks and timestamps to capture the lagged data. | C |
| 2024 / T1 Q64 | You are integrating one of your internal IT applications and Google BigQuery, so users can query BigQuery | I chose this cue because 'You are integrating one of your internal IT applications and Google BigQuery, so users can query BigQuery' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Create a service account and grant dataset access to that account. Use the service account's private key to access the dataset | C |
| 2024 / T1 Q73 | You are designing storage for two relational tables that are part of a 10-TB database on Google Cloud. You | I chose this cue because 'You are designing storage for two relational tables that are part of a 10-TB database on Google Cloud. You' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Use Cloud Spanner for storage. Add secondary indexes to support query patterns. | C |
| 2024 / T1 Q74 | Your financial services company is moving to cloud technology and wants to store 50 TB of financial | I chose this cue because 'Your financial services company is moving to cloud technology and wants to store 50 TB of financial' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Cloud Bigtable | A |
| 2024 / T1 Q75 | An organization maintains a Google BigQuery dataset that contains tables with user-level data. They want to | I chose this cue because 'An organization maintains a Google BigQuery dataset that contains tables with user-level data. They want to' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Create and share an authorized view that provides the aggregate results. | A |
| 2024 / T1 Q77 | Your neural network model is taking days to train. You want to increase the training speed. What can you do? | I chose this cue because 'Your neural network model is taking days to train. You want to increase the training speed. What can you do?' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Subsample your training dataset. | B |
| 2024 / T1 Q79 | Your company maintains a hybrid deployment with GCP, where analytics are performed on your anonymized | I chose this cue because 'Your company maintains a hybrid deployment with GCP, where analytics are performed on your anonymized' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Increase your network bandwidth from your datacenter to GCP. | C |
| 2024 / T1 Q82 | MJTelco Case Study -Company Overview -MJTelco is a startup that plans to build networks in rapidly growing, | I chose this cue because 'MJTelco Case Study -Company Overview -MJTelco is a startup that plans to build networks in rapidly growing,' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Create a partitioned table called tracking_table and include a TIMESTAMP column. | B |
| 2024 / T1 Q85 | You are a head of BI at a large enterprise company with multiple business units that each have different | I chose this cue because 'You are a head of BI at a large enterprise company with multiple business units that each have different' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Switch to flat-rate pricing and establish a hierarchical priority model for your projects. | C |
| 2024 / T1 Q105 | You want to automate execution of a multi-step data pipeline running on Google Cloud. The pipeline includes | I chose this cue because 'You want to automate execution of a multi-step data pipeline running on Google Cloud. The pipeline includes' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Cloud Composer | B |
| 2024 / T1 Q142 | Each analytics team in your organization is running BigQuery jobs in their own projects. You want to enable | I chose this cue because 'Each analytics team in your organization is running BigQuery jobs in their own projects. You want to enable' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Create a Cloud Monitoring dashboard based on the BigQuery metric slots/allocated_for_project | B |
| 2024 / T1 Q152 | You work for a global shipping company. You want to train a model on 40 TB of data to predict which ships in | I chose this cue because 'You work for a global shipping company. You want to train a model on 40 TB of data to predict which ships in' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: BigQuery | A |
| 2024 / T1 Q166 | A shipping company has live package-tracking data that is sent to an Apache Kafka stream in real time. This | I chose this cue because 'A shipping company has live package-tracking data that is sent to an Apache Kafka stream in real time. This' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Implement clustering in BigQuery on the package-tracking ID column. | B |
| 2024 / T1 Q167 | Your company currently runs a large on-premises cluster using Spark, Hive, and HDFS in a colocation facility. | I chose this cue because 'Your company currently runs a large on-premises cluster using Spark, Hive, and HDFS in a colocation facility.' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Migrate the workloads to Dataproc plus Cloud Storage; modernize later. | B |
| 2024 / T1 Q174 | You work for a large financial institution that is planning to use Dialogflow to create a chatbot for the | I chose this cue because 'You work for a large financial institution that is planning to use Dialogflow to create a chatbot for the' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Automate the 10 intents that cover 70% of the requests so that live agents can handle more complicated requests. | A |
| 2024 / T1 Q179 | You are building a real-time prediction engine that streams files, which may contain PII (personal | I chose this cue because 'You are building a real-time prediction engine that streams files, which may contain PII (personal' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Create a pseudonym by replacing PII data with a cryptographic format-preserving token. | D |
| 2024 / T1 Q180 | You are migrating an application that tracks library books and information about each book, such as author or | I chose this cue because 'You are migrating an application that tracks library books and information about each book, such as author or' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Create a table that includes information about the books and authors, but nest the author fields inside the author column. | C |
| 2024 / T1 Q196 | You have 15 TB of data in your on-premises data center that you want to transfer to Google Cloud. Your data | I chose this cue because 'You have 15 TB of data in your on-premises data center that you want to transfer to Google Cloud. Your data' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Install Storage Transfer Service for on-premises data in your data center, and then configure a weekly transfer job. | C |
| 2024 / T1 Q204 | You want to create a machine learning model using BigQuery ML and create an endpoint for hosting the model | I chose this cue because 'You want to create a machine learning model using BigQuery ML and create an endpoint for hosting the model' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Create a Pub/Sub topic and send all vendor data to it. Use Dataflow to process and sanitize the Pub/Sub data and stream it to BigQuery. | D |
| 2024 / T1 Q206 | You need ads data to serve AI models and historical data for analytics. Longtail and outlier data points need | I chose this cue because 'You need ads data to serve AI models and historical data for analytics. Longtail and outlier data points need' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Use Dataflow to identify longtail and outlier data points programmatically, with BigQuery as a sink. | B |
| 2024 / T1 Q207 | You are collecting IoT sensor data from millions of devices across the world and storing the data in | I chose this cue because 'You are collecting IoT sensor data from millions of devices across the world and storing the data in' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Partition table data by create_date, cluster table data by location_id, and device_version. | B |
| 2024 / T1 Q208 | A live TV show asks viewers to cast votes using their mobile phones. The event generates a large volume of | I chose this cue because 'A live TV show asks viewers to cast votes using their mobile phones. The event generates a large volume of' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Write votes to a Pub/Sub topic and load into both Bigtable and BigQuery via a Dataflow pipeline. Query Bigtable for real-time results and BigQuery for later analysis. Shut down the Bigtable instance when voting con... | D |
| 2024 / T1 Q210 | You are designing a data mesh on Google Cloud with multiple distinct data engineering teams building data | I chose this cue because 'You are designing a data mesh on Google Cloud with multiple distinct data engineering teams building data' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: 1. Create a Dataplex virtual lake for each data product, and create multiple zones for landing, raw, and curated data. 2. Provide the data engineering teams with full access to the virtual lake assigned to their da... | D |
| 2024 / T1 Q211 | You are using BigQuery with a multi-region dataset that includes a table with the daily sales volumes. This | I chose this cue because 'You are using BigQuery with a multi-region dataset that includes a table with the daily sales volumes. This' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Schedule a daily export of the table to a Cloud Storage dual or multi-region bucket. | A |
| 2024 / T1 Q212 | You are troubleshooting your Dataflow pipeline that processes data from Cloud Storage to BigQuery. You have | I chose this cue because 'You are troubleshooting your Dataflow pipeline that processes data from Cloud Storage to BigQuery. You have' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Determine whether there is a firewall rule set to allow traffic on TCP ports 12345 and 12346 for the Dataflow network tag. | B |
| 2024 / T1 Q214 | You have a Standard Tier Memorystore for Redis instance deployed in a production environment. You need to | I chose this cue because 'You have a Standard Tier Memorystore for Redis instance deployed in a production environment. You need to' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Create a Standard Tier Memorystore for Redis instance in a development environment. Initiate a manual failover by using the force-data-loss data protection mode. | B |
| 2024 / T1 Q215 | You are administering a BigQuery dataset that uses a customer-managed encryption key (CMEK). You need to | I chose this cue because 'You are administering a BigQuery dataset that uses a customer-managed encryption key (CMEK). You need to' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Copy the tables you need to share to a dataset without CMEKs. Create an Analytics Hub listing for this dataset. | C |
| 2024 / T1 Q216 | You are developing an Apache Beam pipeline to extract data from a Cloud SQL instance by using JdbcIO. You | I chose this cue because 'You are developing an Apache Beam pipeline to extract data from a Cloud SQL instance by using JdbcIO. You' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Set up VPC Network Peering between Project A and Project B. Create a Compute Engine instance without external IP address in Project B on the peered subnet to serve as a proxy server to the Cloud SQL database. | D |
| 2024 / T1 Q217 | You have a BigQuery table that contains customer data, including sensitive information such as names and | I chose this cue because 'You have a BigQuery table that contains customer data, including sensitive information such as names and' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Ensure that the data analytics team members do not have the Data Catalog Fine-Grained Reader role for the policy tags. | B |
| 2024 / T1 Q218 | You have a Cloud SQL for PostgreSQL instance in Region’ with one read replica in Region2 and another read | I chose this cue because 'You have a Cloud SQL for PostgreSQL instance in Region’ with one read replica in Region2 and another read' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Create two new read replicas from the new primary instance, one in Region3 and one in a new region. | C |
| 2024 / T1 Q219 | You orchestrate ETL pipelines by using Cloud Composer. One of the tasks in the Apache Airflow directed | I chose this cue because 'You orchestrate ETL pipelines by using Cloud Composer. One of the tasks in the Apache Airflow directed' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Assign a function with notification logic to the on_failure_callback parameter tor the operator responsible for the task at risk. | C |
| 2024 / T1 Q220 | You are migrating your on-premises data warehouse to BigQuery. One of the upstream data sources resides on a | I chose this cue because 'You are migrating your on-premises data warehouse to BigQuery. One of the upstream data sources resides on a' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Use Datastream to replicate data from your on-premises MySQL database to BigQuery. Set up Cloud Interconnect between your on-premises data center and Google Cloud. Use Private connectivity as the connectivity metho... | B |
| 2024 / T1 Q221 | You store and analyze your relational data in BigQuery on Google Cloud with all data that resides in US | I chose this cue because 'You store and analyze your relational data in BigQuery on Google Cloud with all data that resides in US' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Use the BigQuery Omni functionality and BigLake tables to query files in Azure and AWS. | D |
| 2024 / T1 Q222 | You have a variety of files in Cloud Storage that your data science team wants to use in their models. | I chose this cue because 'You have a variety of files in Cloud Storage that your data science team wants to use in their models.' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Provide the data science team access to Dataprep to prepare, validate, and explore the data within Cloud Storage. | D |
| 2024 / T1 Q223 | You are building an ELT solution in BigQuery by using Dataform. You need to perform uniqueness and null value | I chose this cue because 'You are building an ELT solution in BigQuery by using Dataform. You need to perform uniqueness and null value' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Build Dataform assertions into your code. | C |
| 2024 / T1 Q225 | Your organization stores customer data in an on-premises Apache Hadoop cluster in Apache Parquet format. Data | I chose this cue because 'Your organization stores customer data in an on-premises Apache Hadoop cluster in Apache Parquet format. Data' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Migrate your data to Cloud Storage and migrate the metadata to Dataproc Metastore (DPMS). Refactor Spark pipelines to write and read data on Cloud Storage, and run them on Dataproc Serverless. | A |
| 2024 / T1 Q226 | Your organization has two Google Cloud projects, project A and project B. In project A, you have a Pub/Sub | I chose this cue because 'Your organization has two Google Cloud projects, project A and project B. In project A, you have a Pub/Sub' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Configure VPC Service Controls in the organization with a perimeter around project A. | B |
| 2024 / T1 Q227 | You stream order data by using a Dataflow pipeline, and write the aggregated result to Memorystore. You | I chose this cue because 'You stream order data by using a Dataflow pipeline, and write the aggregated result to Memorystore. You' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Create a new Memorystore for Redis instance with Standard Tier. Set capacity to 5 GB and create multiple read replicas. Delete the old instance. | B |
| 2024 / T1 Q228 | You have a streaming pipeline that ingests data from Pub/Sub in production. You need to update this streaming | I chose this cue because 'You have a streaming pipeline that ingests data from Pub/Sub in production. You need to update this streaming' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Use the Pub/Sub subscription retain-acked-messages flag. | D |
| 2024 / T1 Q229 | You currently use a SQL-based tool to visualize your data stored in BigQuery. The data visualizations require | I chose this cue because 'You currently use a SQL-based tool to visualize your data stored in BigQuery. The data visualizations require' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Create materialized views with the allow_non_incremental_definition option set to true for the visualization queries. Specify the max_staleness parameter to 4 hours and the enable_refresh parameter to true. Referen... | A |
| 2024 / T1 Q230 | You need to modernize your existing on-premises data strategy. Your organization currently uses:• Apache | I chose this cue because 'You need to modernize your existing on-premises data strategy. Your organization currently uses:• Apache' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Use Dataproc to migrate Hadoop clusters to Google Cloud, and Cloud Storage to handle any HDFS use cases. Orchestrate your pipelines with Cloud Composer. | B |
| 2024 / T1 Q231 | You recently deployed several data processing jobs into your Cloud Composer 2 environment. You notice that | I chose this cue because 'You recently deployed several data processing jobs into your Cloud Composer 2 environment. You notice that' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Increase the maximum number of workers and reduce worker concurrency. | C |
| 2024 / T1 Q232 | You are on the data governance team and are implementing security requirements to deploy resources. You need | I chose this cue because 'You are on the data governance team and are implementing security requirements to deploy resources. You need' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Set the constraints/gcp.resourceLocations organization policy constraint to in:europe-west3-locations. | A |
| 2024 / T1 Q233 | You are a BigQuery admin supporting a team of data consumers who run ad hoc queries and downstream reporting | I chose this cue because 'You are a BigQuery admin supporting a team of data consumers who run ad hoc queries and downstream reporting' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Use available administrative resource charts to determine how slots are being used and how jobs are performing over time. Run a query on the INFORMATION_SCHEMA to review query performance. | C |
| 2024 / T1 Q234 | You migrated a data backend for an application that serves 10 PB of historical product data for analytics. | I chose this cue because 'You migrated a data backend for an application that serves 10 PB of historical product data for analytics.' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: 1. Store the historical data in BigQuery for analytics. 2. In a Cloud SQL table, store the last state of the product after every product change. 3. Serve the last state data directly from Cloud SQL to the API. | D |
| 2024 / T1 Q235 | You want to schedule a number of sequential load and transformation jobs. Data files will be added to a Cloud | I chose this cue because 'You want to schedule a number of sequential load and transformation jobs. Data files will be added to a Cloud' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: 1. Create an Apache Airflow directed acyclic graph (DAG) in Cloud Composer with sequential tasks by using the Dataproc and BigQuery operators. 2. Create a separate DAG for each table that needs to go through the pi... | D |
| 2024 / T1 Q236 | You are deploying a MySQL database workload onto Cloud SQL. The database must be able to scale up to support | I chose this cue because 'You are deploying a MySQL database workload onto Cloud SQL. The database must be able to scale up to support' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Create a highly available Cloud SQL instance in region A. Create a highly available read replica in region B. Scale up read workloads by creating cascading read replicas in multiple regions. Promote the read replic... | C |
| 2024 / T1 Q237 | You are planning to load some of your existing on-premises data into BigQuery on Google Cloud. You want to | I chose this cue because 'You are planning to load some of your existing on-premises data into BigQuery on Google Cloud. You want to' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Create your pipeline with Dataflow through the Apache Beam SDK for Python, customizing separate options within your code for streaming, batch processing, and Cloud DLP. Select BigQuery as your data sink. | C |
| 2024 / T1 Q238 | You want to encrypt the customer data stored in BigQuery. You need to implement per-user crypto-deletion on | I chose this cue because 'You want to encrypt the customer data stored in BigQuery. You need to implement per-user crypto-deletion on' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Implement Authenticated Encryption with Associated Data (AEAD) BigQuery functions while storing your data in BigQuery. | A |
| 2024 / T1 Q239 | The data analyst team at your company uses BigQuery for ad-hoc queries and scheduled SQL pipelines in a | I chose this cue because 'The data analyst team at your company uses BigQuery for ad-hoc queries and scheduled SQL pipelines in a' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Update SQL pipelines to run as a batch query, and run ad-hoc queries as interactive query jobs. | B |
| 2024 / T1 Q240 | You are designing a data mesh on Google Cloud by using Dataplex to manage data in BigQuery and Cloud Storage. | I chose this cue because 'You are designing a data mesh on Google Cloud by using Dataplex to manage data in BigQuery and Cloud Storage.' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: 1. Grant the dataplex.dataOwner role to the data engineer group on the customer data lake. 2. Grant the dataplex.dataReader role to the analytic user group on the customer curated zone. | A |
| 2024 / T1 Q241 | You are designing the architecture of your application to store data in Cloud Storage. Your application | I chose this cue because 'You are designing the architecture of your application to store data in Cloud Storage. Your application' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Adopt a dual-region Cloud Storage bucket, and enable turbo replication in your architecture. | C |
| 2024 / T1 Q242 | You have designed an Apache Beam processing pipeline that reads from a Pub/Sub topic. The topic has a message | I chose this cue because 'You have designed an Apache Beam processing pipeline that reads from a Pub/Sub topic. The topic has a message' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: 1. Use a dual-region Cloud Storage bucket with turbo replication enabled. 2. Monitor Dataflow metrics with Cloud Monitoring to determine when an outage occurs. 3. Seek the subscription back in time by 60 minutes to... | D |
| 2024 / T1 Q243 | You are preparing data that your machine learning team will use to train a model using BigQueryML. They want | I chose this cue because 'You are preparing data that your machine learning team will use to train a model using BigQueryML. They want' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C:  | C |
| 2024 / T1 Q244 | Different teams in your organization store customer and performance data in BigQuery. Each team needs to keep | I chose this cue because 'Different teams in your organization store customer and performance data in BigQuery. Each team needs to keep' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Ask each team to publish their data in Analytics Hub. Direct the other teams to subscribe to them. | C |
| 2024 / T1 Q245 | You are developing a model to identify the factors that lead to sales conversions for your customers. You | I chose this cue because 'You are developing a model to identify the factors that lead to sales conversions for your customers. You' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Delineate what data will be used for testing and what will be used for training the model. | C |
| 2024 / T1 Q246 | You have one BigQuery dataset which includes customers’ street addresses. You want to retrieve all | I chose this cue because 'You have one BigQuery dataset which includes customers’ street addresses. You want to retrieve all' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Create a deep inspection job on each table in your dataset with Cloud Data Loss Prevention and create an inspection template that includes the STREET_ADDRESS infoType. | B |
| 2024 / T1 Q247 | Your company operates in three domains: airlines, hotels, and ride-hailing services. Each domain has two | I chose this cue because 'Your company operates in three domains: airlines, hotels, and ride-hailing services. Each domain has two' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: 1. Create one lake for each domain. Inside each lake, create one zone for each team. 2. Attach each of the BigQuery datasets created by the individual teams as assets to the respective zone. 3. Direct each domain t... | C |
| 2024 / T1 Q248 | dataset.inventory_vm sample records:You have an inventory of VM data stored in the BigQuery table. You want | I chose this cue because 'dataset.inventory_vm sample records:You have an inventory of VM data stored in the BigQuery table. You want' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Create a view with a filter to drop rows with fewer than 8 vCPU, and use the UNNEST operator. | A |
| 2024 / T1 Q249 | Your team is building a data lake platform on Google Cloud. As a part of the data foundation design, you are | I chose this cue because 'Your team is building a data lake platform on Google Cloud. As a part of the data foundation design, you are' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Create the bucket with the Autoclass storage class feature. | A |
| 2024 / T1 Q250 | Your company's data platform ingests CSV file dumps of booking and user profile data from upstream sources | I chose this cue because 'Your company's data platform ingests CSV file dumps of booking and user profile data from upstream sources' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: 1. Create a pipeline to de-identify the email field by using recordTransformations in Cloud DLP with format-preserving encryption with FFX as the de-identification transformation type. 2. Load the booking and user... | B |
| 2024 / T1 Q251 | You have important legal hold documents in a Cloud Storage bucket. You need to ensure that these documents | I chose this cue because 'You have important legal hold documents in a Cloud Storage bucket. You need to ensure that these documents' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Set a retention policy. Lock the retention policy. | A |
| 2024 / T1 Q252 | You are designing a data warehouse in BigQuery to analyze sales data for a telecommunication service | I chose this cue because 'You are designing a data warehouse in BigQuery to analyze sales data for a telecommunication service' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Create a denormalized, append-only model with nested and repeated fields. Use the ingestion timestamp to track historical data. | D |
| 2024 / T1 Q253 | You are deploying a batch pipeline in Dataflow. This pipeline reads data from Cloud Storage, transforms the | I chose this cue because 'You are deploying a batch pipeline in Dataflow. This pipeline reads data from Cloud Storage, transforms the' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Ensure that Private Google Access is enabled in the subnetwork. Use Dataflow with only internal IP addresses. | D |
| 2024 / T1 Q254 | You are running a Dataflow streaming pipeline, with Streaming Engine and Horizontal Autoscaling enabled. You | I chose this cue because 'You are running a Dataflow streaming pipeline, with Streaming Engine and Horizontal Autoscaling enabled. You' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Change the pipeline code, and introduce a Reshuffle step to prevent fusion. | B |
| 2024 / T1 Q255 | You have an Oracle database deployed in a VM as part of a Virtual Private Cloud (VPC) network. You want to | I chose this cue because 'You have an Oracle database deployed in a VM as part of a Virtual Private Cloud (VPC) network. You want to' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Create a Datastream service from Oracle to BigQuery, use a private connectivity configuration to the same VPC network, and a connection profile to BigQuery. | D |
| 2024 / T1 Q256 | You are deploying an Apache Airflow directed acyclic graph (DAG) in a Cloud Composer 2 instance. You have | I chose this cue because 'You are deploying an Apache Airflow directed acyclic graph (DAG) in a Cloud Composer 2 instance. You have' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: 1. Enable the Airflow REST API, and set up Cloud Storage notifications to trigger a Cloud Function instance. 2. Create a Private Service Connect (PSC) endpoint. 3. Write a Cloud Function that connects to the Cloud... | C |
| 2024 / T1 Q258 | You have several different file type data sources, such as Apache Parquet and CSV. You want to store the data | I chose this cue because 'You have several different file type data sources, such as Apache Parquet and CSV. You want to store the data' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Use Cloud Data Fusion to move files into Cloud Storage. | B |
| 2024 / T1 Q259 | Your business users need a way to clean and prepare data before using the data for analysis. Your business | I chose this cue because 'Your business users need a way to clean and prepare data before using the data for analysis. Your business' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Use Dataprep to clean the data, and write the results to BigQuery. Analyze the data by using Connected Sheets. | A |
| 2024 / T1 Q260 | You have two projects where you run BigQuery jobs:• One project runs production jobs that have strict | I chose this cue because 'You have two projects where you run BigQuery jobs:• One project runs production jobs that have strict' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Create two reservations, one for each of the projects. For the SLA project, use an Enterprise Edition with a baseline of 300 slots and enable autoscaling up to 500 slots. For the ad-hoc project, configure on-demand... | B |
| 2024 / T1 Q261 | You want to migrate your existing Teradata data warehouse to BigQuery. You want to move the historical data | I chose this cue because 'You want to migrate your existing Teradata data warehouse to BigQuery. You want to move the historical data' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Use BigQuery Data Transfer Service by using the Java Database Connectivity (JDBC) driver with FastExport connection. | A |
| 2024 / T1 Q262 | You are on the data governance team and are implementing security requirements. You need to encrypt all your | I chose this cue because 'You are on the data governance team and are implementing security requirements. You need to encrypt all your' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Create the encryption key in the on-premises HSM and link it to a Cloud External Key Manager (Cloud EKM) key. Associate the created Cloud KMS key while creating the BigQuery resources. | B |
| 2024 / T1 Q263 | You maintain ETL pipelines. You notice that a streaming pipeline running on Dataflow is taking a long time to | I chose this cue because 'You maintain ETL pipelines. You notice that a streaming pipeline running on Dataflow is taking a long time to' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Insert a Reshuffle operation after each processing step, and monitor the execution details in the Dataflow console. | A |
| 2024 / T1 Q264 | You are running your BigQuery project in the on-demand billing model and are executing a change data capture | I chose this cue because 'You are running your BigQuery project in the on-demand billing model and are executing a change data capture' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Create a BigQuery reservation for the project. | D |
| 2024 / T1 Q265 | You are designing a fault-tolerant architecture to store data in a regional BigQuery dataset. You need to | I chose this cue because 'You are designing a fault-tolerant architecture to store data in a regional BigQuery dataset. You need to' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Access historical data by using time travel in BigQuery. | A |
| 2024 / T1 Q266 | You are building a streaming Dataflow pipeline that ingests noise level data from hundreds of sensors placed | I chose this cue because 'You are building a streaming Dataflow pipeline that ingests noise level data from hundreds of sensors placed' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Use session windows with a 15-minute gap duration. | A |
| 2024 / T1 Q267 | You are creating a data model in BigQuery that will hold retail transaction data. Your two largest tables, | I chose this cue because 'You are creating a data model in BigQuery that will hold retail transaction data. Your two largest tables,' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Create a sales_transaction table that holds the sales_transaction_header information as rows and the sales_transaction_line rows as nested and repeated fields. | A |
| 2024 / T1 Q268 | You created a new version of a Dataflow streaming data ingestion pipeline that reads from Pub/Sub and writes | I chose this cue because 'You created a new version of a Dataflow streaming data ingestion pipeline that reads from Pub/Sub and writes' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Drain the old pipeline, then start the new pipeline. | C |
| 2024 / T1 Q269 | Your organization's data assets are stored in BigQuery, Pub/Sub, and a PostgreSQL instance running on Compute | I chose this cue because 'Your organization's data assets are stored in BigQuery, Pub/Sub, and a PostgreSQL instance running on Compute' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Use Data Catalog to automatically catalog BigQuery datasets and Pub/Sub topics. Use Data Catalog APIs to manually catalog PostgreSQL tables. | B |
| 2024 / T1 Q270 | You need to create a SQL pipeline. The pipeline runs an aggregate SQL transformation on a BigQuery table | I chose this cue because 'You need to create a SQL pipeline. The pipeline runs an aggregate SQL transformation on a BigQuery table' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Create a BigQuery scheduled query to run the SQL transformation with schedule options that repeats every two hours, and enable notification to Pub/Sub topic. Use Pub/Sub and Cloud Functions to send an email after t... | D |
| 2024 / T1 Q271 | You are monitoring your organization’s data lake hosted on BigQuery. The ingestion pipelines read data from | I chose this cue because 'You are monitoring your organization’s data lake hosted on BigQuery. The ingestion pipelines read data from' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: 1. Check for duplicate rows in the BigQuery tables that have the daily partition data size doubled. 2. Check the BigQuery Audit logs to find job IDs. 3. Use Cloud Monitoring to determine when the identified Dataflo... | C |
| 2024 / T1 Q272 | You have a BigQuery dataset named “customers”. All tables will be tagged by using a Data Catalog tag template | I chose this cue because 'You have a BigQuery dataset named “customers”. All tables will be tagged by using a Data Catalog tag template' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Create the “gdpr” tag template with public visibility. Assign the bigquery.dataViewer role to the HR group on the tables that contain sensitive data. | C |
| 2024 / T1 Q273 | You are creating the CI/CD cycle for the code of the directed acyclic graphs (DAGs) running in Cloud | I chose this cue because 'You are creating the CI/CD cycle for the code of the directed acyclic graphs (DAGs) running in Cloud' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: 1. Use Cloud Build to copy the code of the DAG to the Cloud Storage bucket of the development instance for DAG testing. 2. If the tests pass, use Cloud Build to copy the code to the bucket of the production instance. | A |
| 2024 / T1 Q274 | You have a BigQuery table that ingests data directly from a Pub/Sub subscription. The ingested data is | I chose this cue because 'You have a BigQuery table that ingests data directly from a Pub/Sub subscription. The ingested data is' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Create a new BigQuery table by using customer-managed encryption keys (CMEK), and migrate the data from the old BigQuery table. | B |
| 2024 / T1 Q275 | You created an analytics environment on Google Cloud so that your data scientist team can explore data | I chose this cue because 'You created an analytics environment on Google Cloud so that your data scientist team can explore data' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Copy the ORC files on Cloud Storage, then create external BigQuery tables for the data scientist team. | D |
| 2024 / T1 Q276 | You are designing a Dataflow pipeline for a batch processing job. You want to mitigate multiple zonal | I chose this cue because 'You are designing a Dataflow pipeline for a batch processing job. You want to mitigate multiple zonal' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Specify a worker region by using the --region flag. | C |
| 2024 / T1 Q277 | You are designing a real-time system for a ride hailing app that identifies areas with high demand for rides | I chose this cue because 'You are designing a real-time system for a ride hailing app that identifies areas with high demand for rides' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Group the data by using a hopping window in a Dataflow pipeline, and write the aggregated data to Memorystore. | B |
| 2024 / T1 Q278 | Your car factory is pushing machine measurements as messages into a Pub/Sub topic in your Google Cloud | I chose this cue because 'Your car factory is pushing machine measurements as messages into a Pub/Sub topic in your Google Cloud' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Use an exception handling block in your Dataflow’s DoFn code to push the messages that failed to be transformed through a side output and to a new Pub/Sub topic. Use Cloud Monitoring to monitor the topic/num_unacke... | B |
| 2024 / T1 Q279 | You want to store your team’s shared tables in a single dataset to make data easily accessible to various | I chose this cue because 'You want to store your team’s shared tables in a single dataset to make data easily accessible to various' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Give analysts the BigQuery Data Viewer role on the shared dataset. Create a dataset for each analyst, and give each analyst the BigQuery Data Editor role at the dataset level for their assigned dataset. | C |
| 2024 / T1 Q280 | You are running a streaming pipeline with Dataflow and are using hopping windows to group the data as the | I chose this cue because 'You are running a streaming pipeline with Dataflow and are using hopping windows to group the data as the' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Use watermarks to define the expected data arrival window. Allow late data as it arrives. | A |
| 2024 / T1 Q281 | You work for a large ecommerce company. You store your customer's order data in Bigtable. You have a garbage | I chose this cue because 'You work for a large ecommerce company. You store your customer's order data in Bigtable. You have a garbage' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Use a timestamp range filter in the query to fetch the customer's data for a specific range. | B |
| 2024 / T1 Q282 | You are using a Dataflow streaming job to read messages from a message bus that does not support exactly-once | I chose this cue because 'You are using a Dataflow streaming job to read messages from a message bus that does not support exactly-once' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Use the BigQuery Storage Write API and ensure that your target BigQuery table is multiregional. | B |
| 2024 / T1 Q283 | You have created an external table for Apache Hive partitioned data that resides in a Cloud Storage bucket, | I chose this cue because 'You have created an external table for Apache Hive partitioned data that resides in a Cloud Storage bucket,' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Upgrade the external table to a BigLake table. Enable metadata caching for the table. | C |
| 2024 / T1 Q284 | You have a network of 1000 sensors. The sensors generate time series data: one metric per sensor per second, | I chose this cue because 'You have a network of 1000 sensors. The sensors generate time series data: one metric per sensor per second,' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Store your data in Bigtable. Concatenate the sensor ID and timestamp and use it as the row key. Perform an export to BigQuery every day. | B |
| 2024 / T1 Q285 | You have 100 GB of data stored in a BigQuery table. This data is outdated and will only be accessed one or | I chose this cue because 'You have 100 GB of data stored in a BigQuery table. This data is outdated and will only be accessed one or' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: 1. Perform a BigQuery export to a Cloud Storage bucket with archive storage class. 2. Set a locked retention policy on the bucket. 3. Create a BigQuery external table on the exported files. | D |
| 2024 / T1 Q286 | You have thousands of Apache Spark jobs running in your on-premises Apache Hadoop cluster. You want to | I chose this cue because 'You have thousands of Apache Spark jobs running in your on-premises Apache Hadoop cluster. You want to' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Move your data to Cloud Storage. Run your jobs on Dataproc. | D |
| 2024 / T1 Q287 | You are administering shared BigQuery datasets that contain views used by multiple teams in your | I chose this cue because 'You are administering shared BigQuery datasets that contain views used by multiple teams in your' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Create a BigQuery reservation with a baseline of 500 slots with no autoscaling for the marketing team, and bill them back accordingly. | C |
| 2024 / T1 Q288 | You are part of a healthcare organization where data is organized and managed by respective data owners in | I chose this cue because 'You are part of a healthcare organization where data is organized and managed by respective data owners in' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Use Dataplex to manage data, track data lineage, and perform data quality validation. | D |
| 2024 / T1 Q289 | You have data located in BigQuery that is used to generate reports for your company. You have noticed some | I chose this cue because 'You have data located in BigQuery that is used to generate reports for your company. You have noticed some' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Use Cloud Data Fusion and Wrangler to normalize the data, and set up a recurring job. | A |
| 2024 / T1 Q290 | You are designing a messaging system by using Pub/Sub to process clickstream data with an event-driven | I chose this cue because 'You are designing a messaging system by using Pub/Sub to process clickstream data with an event-driven' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Use exponential backoff as the subscription retry policy, and configure dead lettering to a different topic with maximum delivery attempts set to 10. | D |
| 2024 / T1 Q291 | You designed a data warehouse in BigQuery to analyze sales data. You want a self-serving, low-maintenance, | I chose this cue because 'You designed a data warehouse in BigQuery to analyze sales data. You want a self-serving, low-maintenance,' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Create an Analytics Hub private exchange, and publish the sales dataset. | A |
| 2024 / T1 Q292 | You have terabytes of customer behavioral data streaming from Google Analytics into BigQuery daily. Your | I chose this cue because 'You have terabytes of customer behavioral data streaming from Google Analytics into BigQuery daily. Your' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Create streams in Datastream to replicate the required tables from both Cloud SQL databases to BigQuery for these queries. | C |
| 2024 / T1 Q293 | Your organization is modernizing their IT services and migrating to Google Cloud. You need to organize the | I chose this cue because 'Your organization is modernizing their IT services and migrating to Google Cloud. You need to organize the' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: 1. Create multiple projects for storage of the data for each of your departments’ applications. 2. Enable each department to create Cloud Storage buckets and BigQuery datasets. 3. In Dataplex, map each department t... | D |
| 2024 / T1 Q294 | You work for a large ecommerce company. You are using Pub/Sub to ingest the clickstream data to Google Cloud | I chose this cue because 'You work for a large ecommerce company. You are using Pub/Sub to ingest the clickstream data to Google Cloud' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Set the topic retention policy to 30 days. | B |
| 2024 / T1 Q295 | You are designing the architecture to process your data from Cloud Storage to BigQuery by using Dataflow. The | I chose this cue because 'You are designing the architecture to process your data from Cloud Storage to BigQuery by using Dataflow. The' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Assign the compute.networkUser role to the service account that executes the Dataflow pipeline. | B |
| 2024 / T1 Q297 | You migrated your on-premises Apache Hadoop Distributed File System (HDFS) data lake to Cloud Storage. The | I chose this cue because 'You migrated your on-premises Apache Hadoop Distributed File System (HDFS) data lake to Cloud Storage. The' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: 1. Define a BigLake table. 2. Create a taxonomy of policy tags in Data Catalog. 3. Add policy tags to columns. 4. Process with the Spark-BigQuery connector or BigQuery SQL. | B |
| 2024 / T1 Q298 | One of your encryption keys stored in Cloud Key Management Service (Cloud KMS) was exposed. You need to re- | I chose this cue because 'One of your encryption keys stored in Cloud Key Management Service (Cloud KMS) was exposed. You need to re-' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Create a new Cloud KMS key. Create a new Cloud Storage bucket configured to use the new key as the default CMEK key. Copy all objects from the old bucket to the new bucket without specifying a key. | D |
| 2024 / T1 Q299 | You have an upstream process that writes data to Cloud Storage. This data is then read by an Apache Spark job | I chose this cue because 'You have an upstream process that writes data to Cloud Storage. This data is then read by an Apache Spark job' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: 1. Create a dual-region Cloud Storage bucket in the us-central1 and us-south1 regions. 2. Enable turbo replication. 3. Run the Dataproc cluster in a zone in the us-central1 region, reading from the bucket in the sa... | D |
| 2024 / T1 Q300 | You currently have transactional data stored on-premises in a PostgreSQL database. To modernize your data | I chose this cue because 'You currently have transactional data stored on-premises in a PostgreSQL database. To modernize your data' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Migrate your workloads to AlloyDB for PostgreSQL. | B |
| 2024 / T1 Q301 | You are architecting a data transformation solution for BigQuery. Your developers are proficient with SQL and | I chose this cue because 'You are architecting a data transformation solution for BigQuery. Your developers are proficient with SQL and' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Use Dataform to build, manage, and schedule SQL pipelines. | A |
| 2024 / T1 Q302 | You work for a farming company. You have one BigQuery table named sensors, which is about 500 MB and contains | I chose this cue because 'You work for a farming company. You have one BigQuery table named sensors, which is about 500 MB and contains' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: 1. Create a metrics table partitioned by timestamp. 2. Create a sensorId column in the metrics table, that points to the id column in the sensors table. 3. Use an INSERT statement every 30 seconds to append new met... | C |
| 2024 / T1 Q303 | You are managing a Dataplex environment with raw and curated zones. A data engineering team is uploading JSON | I chose this cue because 'You are managing a Dataplex environment with raw and curated zones. A data engineering team is uploading JSON' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Move the JSON and CSV files to the raw zone. | A |
| 2024 / T1 Q304 | You have a table that contains millions of rows of sales data, partitioned by date. Various applications and | I chose this cue because 'You have a table that contains millions of rows of sales data, partitioned by date. Various applications and' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Create a materialized view to aggregate the base table data. Include a filter clause to specify the last one year of partitions. | A |
| 2024 / T1 Q305 | Your organization uses a multi-cloud data storage strategy, storing data in Cloud Storage, and data in Amazon | I chose this cue because 'Your organization uses a multi-cloud data storage strategy, storing data in Cloud Storage, and data in Amazon' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Setup a BigQuery Omni connection to the AWS S3 bucket data. Create BigLake tables over the Cloud Storage and S3 data and query the data using BigQuery directly. | A |
| 2024 / T1 Q306 | You are preparing an organization-wide dataset. You need to preprocess customer data stored in a restricted | I chose this cue because 'You are preparing an organization-wide dataset. You need to preprocess customer data stored in a restricted' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Use Dataflow and the Cloud Data Loss Prevention API to mask sensitive data. Write the processed data in BigQuery. | A |
| 2024 / T1 Q307 | You need to connect multiple applications with dynamic public IP addresses to a Cloud SQL instance. You | I chose this cue because 'You need to connect multiple applications with dynamic public IP addresses to a Cloud SQL instance. You' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Leave the Authorized Network empty. Use Cloud SQL Auth proxy on all applications. | C |
| 2024 / T1 Q308 | You are migrating a large number of files from a public HTTPS endpoint to Cloud Storage. The files are | I chose this cue because 'You are migrating a large number of files from a public HTTPS endpoint to Cloud Storage. The files are' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Create a new TSV file for the remaining files by generating signed URLs with a longer validity period. Split the TSV file into multiple smaller files and submit them as separate Storage Transfer Service jobs in par... | C |
| 2024 / T1 Q309 | You work for an airline and you need to store weather data in a BigQuery table. Weather data will be used as | I chose this cue because 'You work for an airline and you need to store weather data in a BigQuery table. Weather data will be used as' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Create a BigQuery table partitioned by datetime value of the weather date. Set up partition expiration to 30 days. | B |
| 2024 / T1 Q310 | You need to look at BigQuery data from a specific table multiple times a day. The underlying table you are | I chose this cue because 'You need to look at BigQuery data from a specific table multiple times a day. The underlying table you are' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Create a materialized view based off of the query being run. | D |
| 2024 / T1 Q311 | Your chemical company needs to manually check documentation for customer order. You use a pull subscription | I chose this cue because 'Your chemical company needs to manually check documentation for customer order. You use a pull subscription' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Use Pub/Sub exactly-once delivery in your pull subscription. | C |
| 2024 / T1 Q313 | You want to migrate an Apache Spark 3 batch job from on-premises to Google Cloud. You need to minimally | I chose this cue because 'You want to migrate an Apache Spark 3 batch job from on-premises to Google Cloud. You need to minimally' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Execute as a Dataproc Serverless job. | D |
| 2024 / T1 Q314 | You are configuring networking for a Dataflow job. The data pipeline uses custom container images with the | I chose this cue because 'You are configuring networking for a Dataflow job. The data pipeline uses custom container images with the' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Disable external IP addresses from worker VMs and enable Private Google Access. | A |
| 2024 / T1 Q316 | You are administering a BigQuery on-demand environment. Your business intelligence tool is submitting | I chose this cue because 'You are administering a BigQuery on-demand environment. Your business intelligence tool is submitting' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Build materialized views on top of the sales table to aggregate data at the day and month level. | C |
| 2024 / T1 Q317 | You have several different unstructured data sources, within your on-premises data center as well as in the | I chose this cue because 'You have several different unstructured data sources, within your on-premises data center as well as in the' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Use Cloud Data Fusion to move files into Cloud Storage. | D |
| 2024 / T1 Q319 | You are preparing an organization-wide dataset. You need to preprocess customer data stored in a restricted | I chose this cue because 'You are preparing an organization-wide dataset. You need to preprocess customer data stored in a restricted' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Use Dataflow and the Cloud Data Loss Prevention API to mask sensitive data. Write the processed data in BigQuery. | C |
| 2025 / T1 Q209 | A shipping company has live package-tracking data that is sent to an Apache Kafka stream in real time. This | I chose this cue because 'A shipping company has live package-tracking data that is sent to an Apache Kafka stream in real time. This' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Implement clustering in BigQuery on the package-tracking ID column. | B |
| 2025 / T1 Q213 | Your company's customer_order table in BigQuery stores the order history for 10 million customers, with a | I chose this cue because 'Your company's customer_order table in BigQuery stores the order history for 10 million customers, with a' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Cluster the table by country and username fields. | A |
| 2025 / T1 Q257 | You are planning to use Cloud Storage as part of your data lake solution. The Cloud Storage bucket will | I chose this cue because 'You are planning to use Cloud Storage as part of your data lake solution. The Cloud Storage bucket will' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Create a Cloud Storage bucket with Autoclass enabled. | A |
| 2025 / T1 Q296 | Your infrastructure team has set up an interconnect link between Google Cloud and the on-premises network. | I chose this cue because 'Your infrastructure team has set up an interconnect link between Google Cloud and the on-premises network.' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Use Dataflow, write a pipeline that reads the data from Kafka, and writes the data to BigQuery. | C |
| 2025 / T1 Q312 | You are migrating your on-premises data warehouse to BigQuery. As part of the migration, you want to | I chose this cue because 'You are migrating your on-premises data warehouse to BigQuery. As part of the migration, you want to' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Use Analytics Hub to facilitate data sharing. | A |
| 2025 / T1 Q315 | You are using Workflows to call an API that returns a 1KB JSON response, apply some complex business logic on | I chose this cue because 'You are using Workflows to call an API that returns a 1KB JSON response, apply some complex business logic on' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Invoke a Cloud Function instance that uses Python to apply the logic on your JSON file. | C |
| 2025 / T1 Q318 | You are using BigQuery with a regional dataset that includes a table with the daily sales volumes. This table | I chose this cue because 'You are using BigQuery with a regional dataset that includes a table with the daily sales volumes. This table' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Schedule a daily export of the table to a Cloud Storage dual or multi-region bucket. | A |
| 2026 / T1 Q320 | Your company uses Looker Studio connected to BigQuery for reporting. Users are experiencing slow dashboard | I chose this cue because 'Your company uses Looker Studio connected to BigQuery for reporting. Users are experiencing slow dashboard' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer BD: B: Create a materialized view in BigQuery that pre-calculates the aggregations and filters used in the Looker Studio dashboards. | D: Use BigQuery BI Engine to accelerate query performance by caching frequently access... | BD |
| 2026 / T1 Q321 | Your company has data assets across multiple Cloud Storage buckets and BigQuery datasets containing raw and | I chose this cue because 'Your company has data assets across multiple Cloud Storage buckets and BigQuery datasets containing raw and' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: 1. Use Dataplex to organize the BigQuery datasets and Cloud Storage buckets into lakes and zones. 2. Use Dataplex for automated metadata discovery, centralized security policy management, data profiling, and data q... | B |
| 2026 / T1 Q322 | Your company is planning to migrate a large on-premises data warehouse to BigQuery. The data is currently | I chose this cue because 'Your company is planning to migrate a large on-premises data warehouse to BigQuery. The data is currently' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Export the data to CSV files, upload the files to Cloud Storage, then load the files into BigQuery. | B |
| 2026 / T1 Q323 | Your company uses separate Google Cloud projects for development, staging, and production. Developers need | I chose this cue because 'Your company uses separate Google Cloud projects for development, staging, and production. Developers need' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Create Google Groups for access levels, assign developers to groups, and grant the groups the appropriate IAM roles on each project. | D |
| 2026 / T1 Q324 | Your financial services company has a critical daily reconciliation process that involves several distinct | I chose this cue because 'Your financial services company has a critical daily reconciliation process that involves several distinct' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Implement a Cloud Composer DAG, with each step defined as a separate task using appropriate Airflow operators, and schedule the DAG for daily execution. | B |
| 2026 / T1 Q325 | Your large e-commerce company uses BigQuery for its data warehousing needs. They have two primary types of | I chose this cue because 'Your large e-commerce company uses BigQuery for its data warehousing needs. They have two primary types of' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Use BigQuery editions to create an autoscaling reservation with dedicated slots for analyst dashboards, while running ETL jobs with a separate reservation or using the on-demand billing model. | A |
| 2026 / T1 Q326 | Your team runs a complex analytical query daily that processes terabytes of data. Recently, after running for | I chose this cue because 'Your team runs a complex analytical query daily that processes terabytes of data. Recently, after running for' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Move from BigQuery on-demand to slot reservations. | A |
| 2026 / T1 Q327 | Your company wants to implement a Retrieval-Augmented Generation (RAG) system to allow employees to query an | I chose this cue because 'Your company wants to implement a Retrieval-Augmented Generation (RAG) system to allow employees to query an' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Convert the unstructured documents into high-dimensional numerical vectors that capture the semantic meaning and relationships of the text. | C |
| 2026 / T1 Q328 | You need to load a dataset with multiple terabytes of clickstream data into BigQuery. The data arrives each | I chose this cue because 'You need to load a dataset with multiple terabytes of clickstream data into BigQuery. The data arrives each' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Create an external table in BigQuery pointing to the Cloud Storage bucket and run the INSERT INTO ... SELECT * FROM external_table command. | A |
| 2026 / T1 Q329 | Your Cloud Storage data lake has raw, processed, and historical data in different buckets. Data older than | I chose this cue because 'Your Cloud Storage data lake has raw, processed, and historical data in different buckets. Data older than' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Configure an Object Lifecycle Management rule to transition objects older than two years to the Archive storage class and eventually delete them after seven years. | B |
| 2026 / T1 Q330 | Your data science team needs to perform interactive SQL queries on large datasets stored in Apache Parquet | I chose this cue because 'Your data science team needs to perform interactive SQL queries on large datasets stored in Apache Parquet' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Deploy a Dataproc cluster with Hive services enabled. | D |
| 2026 / T1 Q331 | You analyze user clickstream data to personalize content recommendations. The data arrives continuously and | I chose this cue because 'You analyze user clickstream data to personalize content recommendations. The data arrives continuously and' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Use Pub/Sub for ingestion, Dataflow with Apache Beam for processing, and BigQuery for storage and analytics. | C |
| 2026 / T1 Q332 | You are designing BigQuery tables for large volumes of clickstream event data. Your data analyst team will | I chose this cue because 'You are designing BigQuery tables for large volumes of clickstream event data. Your data analyst team will' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Partition the table by the event date column and cluster the table by user ID column. | D |
| 2026 / T1 Q333 | You are building a Dataflow pipeline to ingest customer feedback. Before loading to your data warehouse, you | I chose this cue because 'You are building a Dataflow pipeline to ingest customer feedback. Before loading to your data warehouse, you' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Apply a ParDo transform in Dataflow to validate each element, use a RunInference transform to assign sentiment scores, and use side outputs to route valid/invalid records. | D |
| 2026 / T1 Q334 | You need to orchestrate a pipeline with several Google Cloud services: a batch Dataflow job, then a BigQuery | I chose this cue because 'You need to orchestrate a pipeline with several Google Cloud services: a batch Dataflow job, then a BigQuery' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Select Cloud Workflows. | D |
| 2026 / T1 Q335 | You have a data analyst team member who needs to analyze data by using BigQuery. The data analyst wants to | I chose this cue because 'You have a data analyst team member who needs to analyze data by using BigQuery. The data analyst wants to' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Create a pipeline by using BigQuery pipelines and schedule it to load the data into BigQuery daily. | D |
| 2026 / T1 Q336 | You are designing a stateful data processing pipeline that reads data from a Cloud Storage bucket and writes | I chose this cue because 'You are designing a stateful data processing pipeline that reads data from a Cloud Storage bucket and writes' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Launch the Dataflow job with the --region=us-central1 parameter. | A |
| 2026 / T1 Q337 | Your team has created several BigQuery curated datasets containing anonymized industry benchmark data. You | I chose this cue because 'Your team has created several BigQuery curated datasets containing anonymized industry benchmark data. You' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer A: A: Publish the datasets as listings within BigQuery sharing (Analytics Hub). | A |
| 2026 / T1 Q338 | Your organization stores highly personal data in BigQuery and needs to comply with strict data privacy | I chose this cue because 'Your organization stores highly personal data in BigQuery and needs to comply with strict data privacy' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Use AEAD functions and delete keys when employees leave the organization. | D |
| 2026 / T1 Q339 | You monitor and optimize the BigQuery instance for your team. You notice that a particular daily report that | I chose this cue because 'You monitor and optimize the BigQuery instance for your team. You notice that a particular daily report that' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Leverage BigQuery's Query History view and analyze the execution graph. | D |
| 2026 / T1 Q340 | Your company stores vital operational sales data in a BigQuery dataset in us-central1. Your company requires | I chose this cue because 'Your company stores vital operational sales data in a BigQuery dataset in us-central1. Your company requires' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Configure BigQuery cross-region dataset replication from ns-central1 to us-east1. | C |
| 2026 / T1 Q341 | You are integrating your legacy on-premises MySQL database into a new Google Cloud data warehouse. The | I chose this cue because 'You are integrating your legacy on-premises MySQL database into a new Google Cloud data warehouse. The' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Configure a scheduled transfer in BigQuery Data Transfer Service to ingest data from the MySQL database with a scheduled transfer configuration. | B |
| 2026 / T1 Q342 | You are developing a fraud detection model using BigQuery ML. You have a raw transaction dataset and need to | I chose this cue because 'You are developing a fraud detection model using BigQuery ML. You have a raw transaction dataset and need to' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Use the TRANSFORM clause within the CREATE MODEL statement, leveraging SQL functions for aggregations and time-based calculations. | C |
| 2026 / T1 Q343 | Your organization has thousands of images stored in Cloud Storage. The organization's data analysts need to | I chose this cue because 'Your organization has thousands of images stored in Cloud Storage. The organization's data analysts need to' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Use Vertex AI Vision API to analyze the images and store the results in BigQuery. | C |
| 2026 / T1 Q344 | You are building a system to process financial transactions. The system must handle a high throughput of | I chose this cue because 'You are building a system to process financial transactions. The system must handle a high throughput of' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Select Spanner. | D |
| 2026 / T1 Q345 | You are preparing data to serve a sales demand prediction model. The training data undergoes several | I chose this cue because 'You are preparing data to serve a sales demand prediction model. The training data undergoes several' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Replicate the exact same pre-processing logic in the inference pipeline that was used during model training. | B |
| 2026 / T1 Q346 | Your retail company is concerned about their BigQuery analytics spend. The company runs several queries that | I chose this cue because 'Your retail company is concerned about their BigQuery analytics spend. The company runs several queries that' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer B: B: Create a materialized view to minimize repetitive computations. | B |
| 2026 / T1 Q347 | You are defining the data governance strategy for a new BigQuery table with medical and financial data. You | I chose this cue because 'You are defining the data governance strategy for a new BigQuery table with medical and financial data. You' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Create separate datasets with authorized views exposing only approved data. | D |
| 2026 / T1 Q348 | You are designing a data lake on Google Cloud to store vast amounts of customer interaction data from various | I chose this cue because 'You are designing a data lake on Google Cloud to store vast amounts of customer interaction data from various' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer C: C: Use Cloud Storage and BigQuery as repositories. Use Dataplex Universal Catalog for metadata discovery, data quality checks, and transformations. | C |
| 2026 / T1 Q349 | You are using Dataflow to build a streaming data pipeline to analyze user website click activity from | I chose this cue because 'You are using Dataflow to build a streaming data pipeline to analyze user website click activity from' is the decision trigger in the question, not just a product name from the answer. It tells you which constraint must be solved, so map it to answer D: D: Use session windows with a 30-minute gap duration. | D |
